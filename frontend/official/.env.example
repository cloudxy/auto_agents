# 前端环境变量配置

## 开发环境 (.env.development)
```
REACT_APP_API_BASE_URL=http://localhost:9111/api/v1
REACT_APP_ENV=development
```

不设置该变量时，代码回退到默认值 `http://localhost:9111/api/v1`
（后端端口以 `config/default/api.yml` 为准）。

## 生产环境 (.env.production)
```
REACT_APP_API_BASE_URL=https://api.example.com/api/v1
REACT_APP_ENV=production
```

## 使用方式

```typescript
const apiUrl = process.env.REACT_APP_API_BASE_URL
```

**注意**：
- 环境变量必须以 `REACT_APP_` 开头
- 修改 `.env` 文件后需要重启开发服务器
