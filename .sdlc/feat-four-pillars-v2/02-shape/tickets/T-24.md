# T-24 · 详情正文纯文本；迁 XSS 合同；出处不泄漏未上架父插件

> 泳道：L4｜波次：Wave 1｜角色：`/frontend` official｜粗估：1d｜状态：**done**
> 合同锁：**v1.6**
> 上游：
> - PRD：`spec.md` **v1.6** FR-32.4 / FR-32.5
> - 方案：`contract.md` **v1.6** §11 T-24
> - ADR：[adr-0018](../adr-0018-listing-vs-runtime.md) 合集边不是礼包；出处链接仅父插件已上架

## FR 锚点

| FR | GWT | 本票是否完整覆盖 |
|---|---|---|
| FR-32 | GWT-32.4, 32.5 | 是（详情数据闸在 T-23） |
| NFR-04 | SKILL.md 纯文本 | 是 |

## 边界

**本票做什么**：不可信正文按纯文本渲染，脚本不执行、无脚本节点；子卡已上架且父插件未上架时，出处 = 纯文本署名，不可点进父插件商店页。把 `SkillsSquare.test.tsx` XSS 合同 **迁**到市场详情页，不断档。

**本票不做什么**：

- 不改商店读模型 / 404 同形（T-23）
- 不实现订阅
- **禁止**删 `frontend/official/src/pages/SkillsSquare.test.tsx` 的 XSS 用例而不把同一合同迁到新页

## 依赖

| 前置票 | 需要它提供什么 |
|---|---|
| T-23 | 详情 JSON：正文、出处、父插件是否公开；未上架父不给可点链接字段 |

## expand-contract

- 无破坏性 API。旧 `SkillsSquare` 页若仍短暂存在，XSS 测试必须在旧页或新页 **至少一处绿**；删除旧页的同一 PR 必须带新页测试。
- 出处：父插件已上架才给出处链接，否则纯文本署名（T-23 字段 + 本票不渲染 `<a>`）。

## Then（合同 v1.6，禁止缩短）

**禁止**删 `SkillsSquare` XSS 测试而不迁。GWT-32.4：脚本源码当文本可见，不执行、无脚本节点。GWT-32.5：纯文本署名，不可点进父插件商店页。禁止「安装此插件将获得全部技能」。

## 验收 GWT

- [x] GWT-32.4：Then 脚本源码当文本可见，不执行、无脚本节点
- [x] GWT-32.5：Then 纯文本署名，不可点进父插件商店页

## 同 PR 测试

- 迁 `SkillsSquare.test.tsx`：`XSS_PAYLOAD = '<script>alert("xss")</script> <img src=x onerror=alert(1)>'`；正文 `textContent` 含 `<script>`；`container.querySelector('script')` 为 null；`img[src="x"]` 为 null。
- 出处：父未上架 → 无指向父插件商店路径的 `<a>`。
- CI：删除旧测试文件前新文件必须已断言上述合同。

## 自测命令

```bash
npm test --prefix frontend/official -- --watchAll=false
npm run build --prefix frontend/official
```
