# T-14 · LiteLLM 独立 compose + 密钥注入 + 禁 :latest；生成配置不进跟踪树

> Wave L｜角色：`/sre`｜粗估：2d｜状态：**done**｜contract **v1.6**
> 上游：`01-define/spec.md` FR-75 · ADR-0010 / ADR-0014 · contract §11 T-14
> SH 锁：SH-01（不把网关焊进根编排/不长期双通道当完成态）· 生成配置离树承接 T-11

## FR 锚

| FR | GWT | 覆盖 |
|---|---|---|
| FR-75 | GWT-75.1, 75.2, 75.3 | 是（运行时注入 + 跟踪树；值班掩码与 T-18 同句） |

**本票做什么**：独立 `deploy/litellm` compose + 自有 PG；密钥只走环境/密钥注入；镜像 tag **本票钉死**（禁 `:latest`，不代选具体 digest 以外的浮动 tag）。根 `docker-compose.yml` **只允许** `external: litellm-net`，**禁止**声明 litellm 服务。PG 不进 Alembic。生成配置不进跟踪树。

**本票不做什么**：不改 `llm_chat` 出口（T-16）；不 resume `services/litellm/` pyc；不把 LiteLLM Admin UI 当租户产品；不把 `/health/deep` 绑网关 liveness。

## 依赖

| 前置票 | 需要它提供什么 |
|---|---|
| T-11 | `config.gen.yaml` 已离树；跟踪树无明文 Key |

T-15 依赖本票 compose/网络契约可引用。

## expand-contract

根 compose 只加 **external 网络名**，不声明服务。切换窗允许短双跑作回滚件，**不是**本票完成态（完成态在 T-20 = GWT-72.1 **且** GWT-70.1）。SALT 轮换不可当普通回滚。

## GWT / Then（不得改写成短句）

- GWT-75.1：Wave L 完成后的发布物与仓库跟踪文件无 LiteLLM/上游明文 Key；网关生成配置不在跟踪树内（轮换证据在交付清单，不在本 GWT 写密钥）。本票先把运行时注入与「生成配置不跟踪」钉死；T-20 退役后仍保持。
- GWT-75.2：超管打开值班页且其中有密钥相关字段 → 仅掩码或无完整上游 Key（与 T-18 同一句；本票保证注入侧不把完整 Key 写进可跟踪文件）。
- GWT-75.3：租户直打值班页走 GWT-07.3，页上无密钥（与 GWT-14.3 同一句）。
- **SH-01 相关（本票边界）**：禁止根 compose 声明 litellm 服务；禁止把「compose 起来了」写成平台路径已切（平台路径 Then 在 T-16 / T-20）。禁止 DSN 抄到 backend。镜像禁 `:latest`。

## 同 PR 测试 / 证据

- `deploy/litellm/docker-compose.yml`（或等价）存在；服务不出现在根 `docker-compose.yml` 的 services 表。
- 根文件仅 `external: litellm-net`。
- `git ls-files` 对网关生成配置 0 命中；tag 钉死在 compose/README（非 latest）。
- README 网络契约同 `deploy/newapi` §5.2 同构，对象换成 litellm。
- 闸：`git grep -n ':latest' deploy/litellm` 0（或仅注释）；`bash scripts/check-arch.sh`。
