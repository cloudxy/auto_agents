# T-15 · `llm_gateway/` 拆 `chat.py` vs `admin.py`；禁 DSN；禁 pyc；落地 B4 全表

> Wave L｜角色：`/backend`｜粗估：2d｜状态：**done**｜contract **v1.6**
> 上游：ADR-0010 / ADR-0014 · contract §2.3 / §11 T-15
> SH 锁：**SH-02** 全表 B4+R10+禁 DSN · **SH-08** 拆 chat/admin、禁「管理客户端」散文 · **SH-10** `power_market/` 同一条 grep 加 `llm_gateway`

## FR 锚

| FR | GWT | 覆盖 |
|---|---|---|
| FR-70 | （基建） | 本票不勾 70.1…70.10 产品格；那些格在 T-16。本票 Then = B4 检查已绿 + 适配叶可被 `llm_client` import chat |

**本票做什么**：新建 `backend/services/llm_gateway/chat.py` 与 `llm_gateway/admin.py`。落地 ADR-0010 **全表 B4**（禁止只扩 `litellm_*`/`relay_*`）。R10 递归 `backend/services/**/*.py`。禁网关 DSN / `create_async_engine` 打网关。禁 resume pyc。`llm_gateway/__init__.py` 禁止把 `chat` 与 `admin` 打进同一 `__all__`。

**本票不做什么**：不改四动作出口解析（T-16）；不把 `llm_chat` 挪出 `ai_planner/`；不写「管理客户端」散文当 B4；不建 `power_market/` 业务（T-21）；市场评分只许走 `llm_chat`（T-16 `POST /api/v1/skills/{name}/rescore`），禁止直 import `llm_gateway.chat` / `admin`。

## 依赖

| 前置票 | 需要它提供什么 |
|---|---|
| T-14 | 独立 compose / 网络 / 禁 latest；backend 只 HTTP |

T-16 / T-18 / T-19 / T-21 依赖本票检查 **已绿**。

## expand-contract

新包名 `llm_gateway/`，禁止 resume `services/litellm/` pyc。B4 与 R10 改动进 `scripts/check-arch.sh` 同 PR。禁止一票把 chat+admin 合成单模块再靠注释分界。

## GWT / Then（SH-02 / SH-08 / SH-10 全文，不得回退短句）

**落地 ADR-0010 全表 B4**，grep **写死**进 `scripts/check-arch.sh`（禁止「管理客户端」散文；禁止扫整个 `ai_planner/` 禁 `llm_gateway`）：

```
# power_market 禁这些前缀（含 llm_gateway：禁 chat/admin 直连）
grep -rnE '^(from|import) backend\.services\.(spider_|newapi_|litellm_|relay_|channel_|ai_planner|llm_gateway)' backend/services/power_market/

# ai_planner 只禁 admin（三模式）
grep -rnE 'from backend\.services\.llm_gateway\.admin|import backend\.services\.llm_gateway\.admin|from backend\.services\.llm_gateway import admin' backend/services/ai_planner/

# ai_planner 除 llm_client.py 禁 chat（三模式）
grep -rnE 'from backend\.services\.llm_gateway\.chat|import backend\.services\.llm_gateway\.chat|from backend\.services\.llm_gateway import chat' backend/services/ai_planner/ --exclude=llm_client.py
```

允许：`backend/services/ai_planner/llm_client.py` 仅 `from backend.services.llm_gateway.chat import ...`。

**SH-10**：`power_market/` 行必须含 `llm_gateway`（同一条 grep 加 `llm_gateway`）。拆 `chat.py` 后 `from llm_gateway.chat import` **不得**过本票。T-21 Then：`grep -rn llm_gateway backend/services/power_market/` **零命中**（本票把检查装上；T-21 建包时保持零命中）。

**SH-08**：`ai_planner/` **只禁 admin**；**允许** `llm_client.py` import chat。禁止把 B4 写成「禁 import 网关管理客户端」。禁止把 `llm_chat` 挪出 `ai_planner/`。

**R10**：扫描从 `backend/services/*.py` 一层扩到 `backend/services/**/*.py`。

**禁 DSN**：lint 扫 `LITELLM.DB_DSN` 与对网关库的 `create_async_engine`。禁止新代码 `create_async_engine(settings["LITELLM.DB_DSN"])`。

**`llm_gateway/__init__.py` 禁止 chat+admin 同 `__all__`。**

## 同 PR 测试

- `scripts/check-arch.sh` 增加 B4 全表（上列三条 grep 原文）+ R10 递归 + `LITELLM.DB_DSN` / 网关 `create_async_engine` 扫描。
- 新文件存在：`backend/services/llm_gateway/chat.py`、`admin.py`；无 pyc-only 包。
- `__init__.py` 断言：`chat` 与 `admin` 不同时在 `__all__`。
- 闸：`bash scripts/check-arch.sh` 退出码 0（B4/R10 新规则绿）。T-21 不得早于本检查已绿。
