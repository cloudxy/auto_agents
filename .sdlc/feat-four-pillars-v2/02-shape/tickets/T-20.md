# T-20 · 退役验收：停 new-api 进程；墓碑文档；`NEWAPI.ENABLED` 恒 false 后删读

> Wave L｜角色：`/sre` + `/backend`｜粗估：1d｜状态：**done**｜contract **v1.8**
> 上游：`01-define/spec.md` FR-72 · ADR-0014 · contract §0 / §11 T-20
> SH 锁：**SH-01** 完成态 Then = **GWT-72.1 且 GWT-70.1**（无自有行 outbound=网关 URL）；禁止只停进程不验 70.1
> 票闸：**TK-03** 收到 GWT-70.4（与 T-01 并收；T-16 禁止单独勾「是」）；**TK-05** 70.1 闸只钉成功格 node；SH-01…18 保持 closed

## FR 锚

| FR | GWT | 覆盖 |
|---|---|---|
| FR-72 | GWT-72.1, 72.2, 72.3 | 是 |
| FR-70 | GWT-70.1 | **完成态叠**（SH-01）：无自有行 outbound=网关 URL，不是 `https://pub` |
| FR-70 | GWT-70.4 | **部分**（与 T-01 并收：Wave L 后后台/产品仍无「直连平台网关 / 我的中转令牌」；T-16 禁止单独勾「是」） |

**本票做什么**：Wave L 完成后 new-api **不在运行时**（进程停止）。默认 `LLM.DATA_PLANE=litellm`。`NEWAPI.ENABLED` 恒 false 后删读。墓碑文档。值班不再依赖 new-api 管理面。随后一笔平台路径规划不得再打到 new-api。

**本票不做什么**：切换窗短双跑不是完成态；不把渠道组写成当前可买（与 GWT-60.1 / FR-01 闭集 B2 同一句，T-01 已撤文案，本票回归）；不代选 Q-RELAY / Q-AGPL。

## 依赖

| 前置票 | 需要它提供什么 |
|---|---|
| T-16 | 四动作平台出口；`test_saas_byok.py::test_no_own_key_falls_back_to_platform` outbound=网关 URL（不是 `https://pub`）；规划成功格 `::test_post_plan_outbound_gateway` |
| T-18 | 值班已是网关列表 / 71.2 / 71.3 |
| T-19 | 探针/窗口已 HTTP，无 DSN |

## expand-contract

默认旗标切到 `litellm` **之后**才停进程。回滚窗内允许 `DATA_PLANE=providers`，T-20 完成后默认 `litellm`。`NEWAPI.*` 读路径：恒 false 后删除读，禁止留死读当运行时。禁止根 compose 把 new-api 再声明回来。

## GWT / Then（不得改写成短句）

- **完成态 Then = GWT-72.1 且 GWT-70.1（SH-01）**。T-20 完成态只停 new-api 不验 GWT-70.1 = 本合同不合格。
- **GWT-72.1**：new-api 进程已停止、值班页已是 FR-71 网关列表，经办做随后一笔平台路径规划 → 该次不经过 new-api。
- **GWT-70.1（叠）**：夹具 = 进程已停 + 值班已是网关列表 + 企业无激活的本企业供应商 + 月度 token 未尽 + 平台网关已登记至少一条模型；When = 经办 `POST /api/v1/ai/plans/{id}/plan`（与 T-16 ① 同一 HTTP，禁止第四夹具、禁止测试内 `llm_chat`）；Then = 该次 outbound URL = 网关 URL（不是 `https://pub`、不是平台公共 `llm_providers`、不是 yml/env、不是 new-api）。成功格等到 outbound，禁止仅 HTTP 200 / `status=planning` 勾。
- **GWT-72.2**：Wave L 已完成，超管打开值班页 → 值班不依赖 new-api 管理面（读到的是 FR-71 的网关列表或 71.2/71.3 空态/降级，不是 new-api 渠道方言）。
- **GWT-72.3**：Wave L 已完成、Q-RELAY 未关闭，租户寻找「我的渠道组 / 我的中转令牌」→ 无该能力；定价无渠道组当前可买（与 GWT-60.1 / FR-01 闭集 B2 同一句）。
- **GWT-70.4**（本票与 T-01 并收）：已登录经办在浏览器寻找「直连平台网关 / 我的中转令牌」→ 无直连平台网关、无我的中转令牌。本票负责 Wave L 完成后的后台壳 / 值班 / 用量入口。**不得与 72.3 写成「同一句」**（70.4 无「直连平台网关」；72.3 无「我的渠道组」+ 定价 B2）。T-16 禁止单独勾「是」。
- 默认 `LLM.DATA_PLANE=litellm`。`NEWAPI.ENABLED` 恒 false 后删读。

## 同 PR 测试

- 进程已停夹具 + T-16 规划成功格 outbound=网关 URL（复用 T-16 ①，禁止 similar-suggest 勾 70.1）。
- 值班页回归 71.1 或 71.2/71.3，无 new-api 渠道方言。
- 默认配置 `LLM.DATA_PLANE=litellm`；`DATA_PLANE=providers` 不得当完成态。
- 官网定价回归：无渠道组当前可买（T-01）。
- **GWT-70.4**：新增 `frontend/admin/src/App.test.tsx::test_operator_no_direct_gateway_or_relay_token_entry`（经办壳无「直连平台网关」、无「我的中转令牌」入口）。未加该 node 之前不得勾 70.4。不得用 72.3 夹具空过 70.4。
- **TK-05**：本票 70.1 闸**只钉成功格** `backend/tests/test_llm_four_actions_http.py::test_post_plan_outbound_gateway`。禁止 `or_cell_failure` 一名三格。禁止用 70.2/74.1 失败格 node 勾 70.1。禁止 `test_trigger_plan_endpoint` 当 70.1。
- 闸：T-16 `backend/tests/test_llm_four_actions_http.py::test_post_plan_outbound_gateway` 仍绿（只 70.1；禁止 `::test_post_plan_no_model_only_70_2` / `::test_post_plan_unreachable_only_74_1` / `or_cell_failure` 当本票 70.1）；sre 停进程 evidence（命令+退出码）；`npm test --prefix frontend/admin -- --testPathPattern=App.test`（70.4 node）。
