# T-19 · 探针/窗口改管理 HTTP；spend→budget；伪装仍不熔断

> Wave L｜角色：`/backend`｜粗估：2d｜状态：**done**｜contract **v1.6**
> 上游：`01-define/spec.md` GWT-07.6 · contract §7.3 / spike T-19 前置
> SH 锁：**SH-06** 与 T-18 同一 expand（string `gateway_ref`；禁改 `channel_id` 类型；只允许 `NEWAPI.*` 读 + `RELAY.*` 写）· GWT-07.6 换调用后仍真

## FR 锚

| FR | GWT | 覆盖 |
|---|---|---|
| FR-07 | GWT-07.6 | 是（换网关管理 HTTP 后仍真） |
| FR-71 | （探针数据面） | 探针走平台网关 `/v1/chat/completions`；列表/空态句在 T-18 |

**本票做什么**：探针/窗口改走 `llm_gateway/admin.py` 管理 HTTP。spend 映射 budget。伪装仍不熔断（渠道保持可用、不自动关闭；窗口与额度不变）。先只读 spend 再打开写。失败则只留「冷却到期自动恢复且不覆盖人工禁用」一条在 backend。**禁止 DSN。**

**本票不做什么**：不改探针阈值（algo N/A）；不 1:1 复刻 new-api 窗口调度；不加 `gateway_ref` 表列（等 `/dba`）；不改 `channel_id` 类型。

## 依赖

| 前置票 | 需要它提供什么 |
|---|---|
| T-15 | `admin.py` + 禁 DSN 已绿 |
| T-18 | 值班列表/信封/string ref 双读已在 |

## expand-contract（SH-06）

HTTP 与 Redis **用 string `gateway_ref`**（expand：`newapi:channel:cfg:{id}` 双读 → `relay:channel:cfg:{ref}`）。表列等 `/dba`。**本票禁止改 `channel_id` 类型。** 配置只允许 `NEWAPI.*` 读 + `RELAY.*` 写，禁止第三前缀。先只读 spend 再打开写。

前置 spike（只读 curl 官方 OpenAPI，无生产 Key）：网关 spend/budget 是否覆盖「冷却到期自动恢复且不覆盖人工禁用」。盖不住则 backend 只保留这一条恢复，仍 HTTP；禁止 DSN。

## GWT / Then（不得改写成短句）

- GWT-07.6：探针将某渠道判为伪装，值班或系统走完该次判定 → 该渠道保持可用、不自动关闭；窗口与额度不变。Wave L 后探针仍走平台网关调用，本条仍真。租户产品面的伪装可见性可留 FR-61 stub；本条只冻「不熔断」。
- 探针调用平台网关 `/v1/chat/completions`（经适配叶），不是 new-api 方言，不是 backend DSN。
- spend→budget：窗口策略优先 budget；若官方 API 盖不住「冷却到期自动恢复且不覆盖人工禁用」，则 **只** 这一条恢复留 backend，仍 HTTP。
- 禁止 `create_async_engine(settings["LITELLM.DB_DSN"])`；lint 已在 T-15，本票不得新增命中。

## 同 PR 测试

- 改 `backend/tests/test_llm_probe.py` / `test_llm_cooldown.py` / `test_channel_config_service.py`：伪装不熔断；冷却恢复不覆盖人工禁用。
- 禁止测试用网关 DSN。
- Redis/HTTP 用 string ref，与 T-18 同一 expand。
- 闸：`uv run pytest -x -q backend/tests/test_llm_probe.py backend/tests/test_llm_cooldown.py`；`bash scripts/check-arch.sh`（禁 DSN 仍绿）。
