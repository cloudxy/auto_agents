# T-02 · 导出仅 CSV/JSON；单次 100 条

> Wave 0｜角色：`/backend` + `/frontend` admin｜粗估：1d｜状态：**todo**｜contract **v1.6**
> 上游：`01-define/spec.md` FR-03 · `02-shape/contract.md` §11 · NFR-02

## FR 锚

| FR | GWT | 覆盖 |
|---|---|---|
| FR-03 | GWT-03.1, 03.2, 03.3, 03.4, 03.5 | 是（03.5 与 T-01 同一句，本票锁后台选项） |

**本票做什么**：结果导出选项只列会成功生成的格式（CSV 或已有 JSON）。列表与导出同一上限 100 条。xlsx 不进选项。

**本票不做什么**：不实现 xlsx；不改出站拉数（T-10）；不把候选当成果（T-08）。

## 依赖

| 前置票 | 需要它提供什么 |
|---|---|
| — | 无 |

**并行**：∥ T-01（官网 03.5 文案）/ T-03 / T-04。

## expand-contract

无表结构变更。上限是查询/导出窗口，不是删历史行。

## GWT / Then（不得改写成短句）

- GWT-03.1：本企业某已完成任务有 30 条非候选结果，经办选择已列出的格式并导出，得到可打开的文件，行数为 30，格式为 CSV 或已有 JSON 之一。
- GWT-03.2：该任务 0 条结果，点导出提示「没有可导出的结果」，不下载空文件。
- GWT-03.3：120 条非候选结果，导出最多 100 条；按钮旁可见「单次最多 100 条」。
- GWT-03.4：企业 A 的经办尝试导出企业 B 的任务结果 → 拒绝；A 的导出未执行。
- GWT-03.5：访客打开功能介绍，不把 Excel/xlsx 写成当前可买（与 T-01 同一句；本票保证后台选项无 xlsx）。

## 同 PR 测试

- `backend/services/spider_query_service.py` `export_results`：`fmt not in ("csv", "json")` 拒绝；流式窗口最多 100 条非候选（`source <> marketplace` 与 T-08 谓词一致，本票至少排除候选）。
- 改 `backend/tests/test_spider_task_flow.py`：保留 xlsx 拒绝；补 0 条不下载空文件；120 条最多 100；跨租户拒绝。
- 改 `frontend/admin/src/pages/Data.tsx`（及任务结果导出入口）：选项无 xlsx；按钮旁「单次最多 100 条」；空结果句「没有可导出的结果」。
- 闸：`uv run pytest -x -q backend/tests/test_spider_task_flow.py`；admin 相关单测若有则同 PR 绿。
