# T-09 · `check_llm_tokens_month` 接到 `llm_chat` 成功路径前

> Wave 0｜角色：`/backend`｜粗估：1d｜状态：**done**｜contract **v1.7**
> 上游：`01-define/spec.md` FR-12 · X-QUOTA · contract §7.1 / §7.2
> 票闸：**TK-03** GWT-74.3 与 GWT-12.4 并格（T-16 禁止单独勾「是」）；SH-01…18 保持 closed

## FR 锚

| FR | GWT | 覆盖 |
|---|---|---|
| FR-12 | GWT-12.1, 12.2, 12.3, 12.6, 12.7, 12.4, 12.5 | 是 |
| FR-74 | GWT-74.3 | **与 GWT-12.4 并格**（同一写权；T-16 禁止单独勾「是」） |

**本票做什么**：打模型前检查本企业月度 token。满则拒绝，用户可见「已达配额上限」+「申请提升配额」。页上无 `QUOTA_EXCEEDED` / 裸 `429` 字样。将满（≥90%）有警告句。套餐未尽而平台成本熔断时，不得套用套餐超限那句话（只走 GWT-12.5）。**不改出口**（不切 LiteLLM）。闸接到真正打模型的成功路径前。

**本票不做什么**：不把出口切到网关（T-16）；不把网关不可达写成套餐句（GWT-74.1 / 74.4 / 74.5 / 74.6 在 T-16）。**GWT-74.3 与本票 GWT-12.4 并格**，不留给 T-16 单独勾「是」。不实现支付（FR-50 / Q-BILL）。

## 依赖

| 前置票 | 需要它提供什么 |
|---|---|
| — | 无 |

T-16 依赖本票闸已在成功路径前。T-12 依赖本票可产 `quota_exceeded` 事件路径。

## expand-contract

内部业务码 `QUOTA_EXCEEDED` 可保留，**禁止渲染**给租户。前端 `Usage.tsx` 现网文案含 `429 QUOTA_EXCEEDED` 必须同 PR 改掉。满额 CTA 不到企业注册。

## GWT / Then（不得改写成短句）

- GWT-12.1：企业月度 token 未尽，经办使用规划/对话，请求不被套餐闸拒绝。
- GWT-12.2：已用 ≥90% 未满，打开用量见「接近上限。超额操作会被拒绝。」不含内部码。
- GWT-12.3：月度 token 已满，再打模型 → 动作不执行；可见「已达配额上限」+「申请提升配额」；页上无 `QUOTA_EXCEEDED`。
- GWT-12.6：月度 token 已满且页上有「申请提升配额」，点该 CTA 不到企业注册；可到联系说明。本波不必支付。
- GWT-12.7：存储已满且页上有去结果库/清理的下一步，点该 CTA 不到企业注册。
- GWT-12.4：只读成员打开用量只读可见进度；不能改套餐。
- **GWT-74.3（与 12.4 并格）**：只读成员打开用量不能改套餐；不能把网关失败写成套餐已满。同一写权、同一夹具。禁止 T-16 单独勾「是」。禁止用 12.4 绿去空过「网关失败写成套餐已满」那半句——并格两半都要断言。
- GWT-12.5：套餐未尽但平台成本熔断，打模型失败，文案不是套餐超限那句（「已达配额上限」+「申请提升配额」）。网关不可达不走本格（见 GWT-74.1 / 74.4 / 74.5 / 74.6）。两格不得互相勾选。

## 同 PR 测试

- `llm_chat` 成功路径前调用 `QuotaService.check_llm_tokens_month`（`backend/services/ai_planner/llm_client.py`）。
- 改 `backend/tests/test_saas_quota.py` / `test_saas_byok.py` `test_platform_fallback_subject_to_token_quota`：满额拒绝发生在真正 HTTP 出站前。
- 改 `frontend/admin/src/pages/Usage.tsx`：用户可见句无 `QUOTA_EXCEEDED`；满额 CTA 不到注册。
- **12.4 / 74.3 并格**（未加之前不得勾这两格）：`backend/tests/test_saas_quota.py::test_readonly_usage_cannot_change_plan_and_cannot_render_gateway_failure_as_quota`；`frontend/admin/src/pages/Usage.test.tsx::test_readonly_usage_no_plan_edit_gateway_failure_not_quota`。
- 闸：

```bash
uv run pytest -x -q \
  backend/tests/test_saas_quota.py \
  backend/tests/test_saas_quota.py::test_readonly_usage_cannot_change_plan_and_cannot_render_gateway_failure_as_quota
```

`test_saas_quota.py` 现网金标不得单独当 74.3 绿闸。Usage 单测：`npm test --prefix frontend/admin -- --testPathPattern=Usage.test`。
