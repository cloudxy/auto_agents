# T-27 · 引用解析忽略子行上架；黑名单跳过+审计

> 泳道：L4｜波次：Wave 1｜角色：`/backend`｜粗估：1d｜状态：**done**
> 合同锁：**v1.6**
> 上游：
> - PRD：`spec.md` **v1.6** FR-36（X-FR35；QA-27 When = 超管或系统引用列表，非租户执行面）
> - 方案：`contract.md` **v1.6** §8 合集边 / §11 T-27
> - ADR：[adr-0018](../adr-0018-listing-vs-runtime.md) 解析忽略 listing；跳过黑名单/软删

## FR 锚点

| FR | GWT | 本票是否完整覆盖 |
|---|---|---|
| FR-36 | GWT-36.1, 36.2, 36.3 | 是 |

## 边界

**本票做什么**：合集边（出处 + 引用，不是礼包）解析：忽略子行 listing_state；黑名单或软删则跳过并记审计。观测点 = 超管或系统给出的「引用列表」。

**本票不做什么**：

- **不**要求租户在站内跑智能体 / 召唤 / 执行引擎（X-FR35）
- 不把解析列表当商店泄漏（未上架商店页仍走 GWT-32.3）
- 不级联安装行（T-25/T-26）
- 许可闸不拆解析（完整格在 T-31 / GWT-42.5；本票至少不因 listing 拆行）

## 依赖

| 前置票 | 需要它提供什么 |
|---|---|
| T-25 | 安装行存在但解析不读安装礼包；合集边不是安装边 |

## expand-contract

- 合集边只表达出处与引用。无破坏性改 uq。
- 解析输出仅超管/系统；租户打开未上架商店页仍 404 同形（复用 T-23）。

## Then（合同 v1.6，禁止缩短）

观测=超管/系统引用列表，非执行引擎。解析时 **忽略** 子行上架态；黑名单或已删除则跳过并记审计。GWT-36.3：租户打开未上架技能的商店页走商店不存在句（与 GWT-32.3 同一句；解析列表不是商店泄漏）。

## 验收 GWT

- [x] GWT-36.1：Then 该技能仍在引用列表（引用技能未上架且非黑名单）
- [x] GWT-36.2：Then 跳过该技能并有审计；商店里两边都不出现
- [x] GWT-36.3：Then 走商店不存在句（与 GWT-32.3 同一句；解析列表不是商店泄漏）

## 同 PR 测试

- When 必须是「超管或系统打开该智能体的引用列表（非租户执行面）」；禁止用租户执行面空过。
- 36.1 未上架子卡仍在列表；36.2 黑名单跳过 + 审计事件/日志可查；商店公开列表两边都不出现（可复用 T-23 夹具）。
- 禁止把「已订租户使用 A 则 A 仍能调用未上架技能」写成执行引擎验收。

## 自测命令

```bash
uv run pytest -x -q backend/tests
bash scripts/check-arch.sh
```
