# T-03 · 注册成功主按钮去登录；企业名非空

> Wave 0｜角色：`/frontend` official + `/backend`｜粗估：1d｜状态：**todo**｜contract **v1.6**
> 上游：`01-define/spec.md` FR-04 · `02-shape/contract.md` §11

## FR 锚

| FR | GWT | 覆盖 |
|---|---|---|
| FR-04 | GWT-04.1, 04.2, 04.3, 04.4 | 是 |

**本票做什么**：开通成功屏主按钮文案为「登录管理后台」，指向后台登录并带回来源；成功文案含企业名与负责人用户名（不是空书名号）。次钮才「再注册一家」。登录页有「没有账号？企业注册」回官网注册。

**本票不做什么**：不埋 `tenant_signup_succeeded` 查询面（T-12）；不代选支付。

## 依赖

| 前置票 | 需要它提供什么 |
|---|---|
| — | 无 |

**并行**：∥ T-01 / T-02 / T-04。T-12 依赖本票注册成功路径可走完。

## expand-contract

无破坏性变更。登录回链 query 可加、不可删现有注册字段。

## GWT / Then（不得改写成短句）

- GWT-04.1：访客提交企业注册且开通成功，成功屏主按钮文案为「登录管理后台」，指向后台登录并带回来源；成功文案含企业名与负责人用户名（不是空书名号）。
- GWT-04.2：注册失败停留在注册页，提示注册未完成，不出现成功屏，不创建可登录企业。
- GWT-04.3：成功屏点次按钮才是「再注册一家」；登录页有「没有账号？企业注册」回官网注册。
- GWT-04.4：已有企业的只读成员打开注册页并提交，不得把别人的企业改成新企业；失败不泄露其它企业是否存在。

## 同 PR 测试

- 改 `frontend/official/src/pages/Register.tsx`：成功屏主按钮不再是「再注册一家」去 `/register`；主按钮去 admin 登录（`REACT_APP_ADMIN_URL`）并带回来源。
- 改 `frontend/admin/src/pages/Login.tsx`：回链「没有账号？企业注册」。
- 后端注册失败不落可登录企业（现有 signup 测试同 PR 补 04.2 / 04.4）。
- 闸：官网/后台相关单测 + `npm run build --prefix frontend/official`。
