# T-17 · BYOK 直连四动作；保存/测试连接不要求网关活；本企业写与 test=`require_operator`

> Wave L｜角色：`/backend` + `/frontend` admin｜粗估：2d｜状态：**done**｜contract **v1.7**
> 上游：`01-define/spec.md` FR-73 · ADR-0017 · contract §11 T-17
> SH 锁：**SH-09** 本企业写与 test=`require_operator` · **SH-11** `/llm` 露出（去掉 `requireAdmin` + operator `menu:llm`）· **SH-14** 试采/评分夹具跟 T-16 · 第四夹具跟 T-16（SH-07）
> 票闸：**TK-01**（本票）；SH-01…18 保持 closed

## FR 锚

| FR | GWT | 覆盖 |
|---|---|---|
| FR-73 | GWT-73.1, 73.5, 73.7, 73.8, 73.9, 73.6, 73.10, 73.11, 73.12, 73.2, 73.3, 73.4 | 是 |
| FR-06 | GWT-06.5 / 06.6 | 经办露出补完 06.5；平台级行仍 06.6 / 73.3 |

**本票做什么**：有本企业激活行 → 四动作直连该行，不经 LiteLLM，不要求 LiteLLM 存活。本企业供应商写与测试连接 = `require_operator`（admin+operator）。经办能进 `/llm` 页并「点」测试连接。平台级行仍拒绝。

**本票不做什么**：不发租户虚拟令牌；不新造「我的渠道组」；不加 `menu:newapi`；不把 `/newapi` 的 `requireAdmin` 拿掉；不新建聊天 UI。

## 依赖

| 前置票 | 需要它提供什么 |
|---|---|
| T-16 | 四条 When HTTP、试采 `_repair_flow` 夹具、评分 `consume_once` 夹具、第四夹具 `POST /skills/similar-suggest` |

T-05 已保持 `/llm` 不整页 404 与 06.5 负责人保存；本票完成经办写/test 与露出。

## expand-contract

022 已 applied 则 **数据回填 UPDATE** `roles.permissions` JSON，禁止改表结构。`_ROLE_PERMISSIONS["operator"]` + Alembic `022` `_SEED_ROLES` operator + `roles` 表 operator 行 **同 PR** 加 `menu:llm`（仍不加 `menu:newapi`）。

## GWT / Then（不得改写成短句）

- GWT-73.1：企业负责人已登录、平台网关进程已停止，保存本企业供应商 → 保存成功，本企业行变更（与 GWT-06.5 同一成功句）。
- GWT-73.5：本企业有激活供应商、月度 token 未尽、平台网关仍在，经办做规划 → 该次出现在本企业供应商侧调用或用量；不出现在网关侧调用或用量；不打平台供应商直连、不打 new-api。**When HTTP = T-16 ①** `POST /api/v1/ai/plans/{id}/plan`。不得用第四夹具勾。成功格等到 outbound URL = 本企业行侧。
- GWT-73.7：同上 Given，经办做试采修复。**When HTTP = T-16 ②** `POST /api/v1/ai/plans/{id}/test`。**73.7/73.10 跟 T-16 试采夹具**（第一次失败进 `_repair_flow`，否则不得勾）。
- GWT-73.8：同上 Given，经办做技能评分。**When HTTP = T-16 ③** `POST /api/v1/skills/{name}/rescore`。**73.8/73.11 跟 T-16 评分夹具**（`consume_once` 或打开 `SKILLS.SCORING.ENABLED` 并等到消费，否则不得勾）。
- GWT-73.9：同上 Given，经办做平台路径聊天。**73.9/73.12 跟 T-16 第四夹具**（经办 `POST /api/v1/skills/similar-suggest`；`require_operator`；≥2 同 category；When 必须是经办 HTTP，禁止测试内 `llm_chat`）。
- GWT-73.6：本企业有激活供应商、月度 token 未尽、平台网关进程已停止，经办做规划 → 该次出现在本企业供应商侧调用或用量；不出现在网关侧；不打平台供应商直连、不打 new-api；失败则为该行自己的失败句（`LLM_PROVIDER_ERROR`）；**不是** GWT-74.1「平台 LLM 网关不可达」；**不是**套餐超限句。**When HTTP = T-16 ①** `POST /api/v1/ai/plans/{id}/plan`（与 73.5 同级）。成功/该行失败格等到 outbound URL = **本企业行侧**（或该行自己的失败句）。**73.6 族网关停不得勾 74.1**。不得用第四夹具勾。
- GWT-73.10：同上 Given，经办做试采修复（须 `_repair_flow`）→ Then 同 73.6 的直连/失败句约束。**When HTTP = T-16 ②** `POST /api/v1/ai/plans/{id}/test`。等到 outbound = **本企业行侧**（或该行自己的失败句）。**73.6 族网关停不得勾 74.1**。
- GWT-73.11：同上 Given，经办做技能评分（须 `consume_once`）→ Then 同 73.6 的直连/失败句约束。**When HTTP = T-16 ③** `POST /api/v1/skills/{name}/rescore`。等到 outbound = **本企业行侧**（或该行自己的失败句）。**73.6 族网关停不得勾 74.1**。
- GWT-73.12：同上 Given，经办做平台路径聊天（第四夹具）→ Then 同 73.6 的直连/失败句约束。等到 outbound = **本企业行侧**（或该行自己的失败句）。**73.6 族网关停不得勾 74.1**。
- GWT-73.2：无本企业激活供应商、平台网关已登记模型，经办做规划 → 走 FR-70 平台路径，不要求本企业行存在。**When HTTP = T-16 ①** `POST /api/v1/ai/plans/{id}/plan`（与 73.5 同级）。
- GWT-73.3：企业负责人激活或改平台级供应商 → 拒绝且平台行不变（与 GWT-06.6 同一句）。
- GWT-73.4：本企业有激活供应商，经办点该行「测试连接」→ 测的是该行自己的地址，不要求 LiteLLM 存活；失败说明连的是本企业行，不是网关空态句。**HTTP 200 仍勾不了「点」**（SH-11）：必须经办能进 `/llm` 页并看见保存/测试。

**SH-09**：本企业供应商写与测试连接 = `require_operator`（admin+operator）；只读仍拒；**平台级行仍 GWT-06.6 / 73.3**。

**SH-11 同 PR 露出**：去掉 `frontend/admin/src/App.tsx` `/llm` 的 `ProtectedRoute requireAdmin`（或改成经办可进：`role===operator` 或持 `menu:llm`）。`/newapi` **仍** `requireAdmin`。`_ROLE_PERMISSIONS["operator"]` + Alembic `022` `_SEED_ROLES` operator + `roles` 表 operator 行同 PR 加 `menu:llm`（**仍不加 `menu:newapi`**）。022 已 applied 则数据回填 UPDATE JSON，禁止改表结构。

## 同 PR 测试

- 作废 `backend/tests/test_llm_provider.py` `test_create_endpoint_rejects_operator` 金标；经办对本企业行写/test **200**；viewer 403；平台行 403。
  **禁止只靠 `test_create_endpoint_rejects_operator` 变绿勾 FR-73。** 该用例只覆盖写面守卫（73.1/73.4 的 HTTP 身份），不覆盖 73.5–73.12 / 73.2 / 73.6 族。
- 改 `frontend/admin/src/pages/LlmProviders.test.tsx`：经办看得到本企业行保存/测试，只读仍无（现网只钉 viewer「仅管理员可管理供应商」不得再当经办完成态）。
- 改 `frontend/admin/src/App.tsx` `/llm` 路由；`backend/app/api/v1/auth.py` `_ROLE_PERMISSIONS["operator"]`；`022` 种子 + 已 applied 库 UPDATE。

**必须新增四动作夹具（未加之前本票闸必须红；禁止只跑 `test_llm_provider.py` 当 FR-73 绿闸）：**

文件：`backend/tests/test_fr73_byok_four_actions_http.py`（仓库里尚无此文件。）

- `test_post_plan_byok_outbound_own_row`（73.5）：When = T-16 ① `POST /api/v1/ai/plans/{id}/plan`；等到 outbound=**本企业行侧**。不得用第四夹具勾。
- `test_post_plan_byok_gateway_down_outbound_own_row_not_74_1`（73.6）：When = T-16 ① 同上；网关停；等到 outbound=**本企业行侧**（或该行自己的失败句）。**不得勾 74.1**。
- `test_post_plan_no_own_row_follows_fr70`（73.2）：When = T-16 ① 同上；无本企业激活行 → 走 FR-70（outbound=网关 URL），不要求本企业行存在。
- `test_post_test_byok_repair_flow_outbound_own_row`（73.7）：When = T-16 ② `POST /api/v1/ai/plans/{id}/test`；须第一次失败进 `_repair_flow`；等到 outbound=**本企业行侧**。
- `test_post_test_byok_gateway_down_repair_flow_not_74_1`（73.10）：When = T-16 ② 同上；须 `_repair_flow`；网关停；outbound=**本企业行侧**（或该行失败句）；**不得勾 74.1**。
- `test_post_rescore_byok_consume_once_outbound_own_row`（73.8）：When = T-16 ③ `POST /api/v1/skills/{name}/rescore`；须 `consume_once`（或开 `SKILLS.SCORING.ENABLED` 等到消费）；等到 outbound=**本企业行侧**。
- `test_post_rescore_byok_gateway_down_consume_once_not_74_1`（73.11）：When = T-16 ③ 同上；须 `consume_once`；网关停；outbound=**本企业行侧**（或该行失败句）；**不得勾 74.1**。
- `test_similar_suggest_byok_outbound_own_row`（73.9）/ `test_similar_suggest_byok_gateway_down_not_74_1`（73.12）：跟 T-16 第四夹具；73.12 网关停不得勾 74.1。

闸（`test_fr73_byok_four_actions_http.py` 未加之前必须红）：

```bash
uv run pytest -x -q \
  backend/tests/test_fr73_byok_four_actions_http.py \
  backend/tests/test_llm_provider.py \
  backend/tests/test_saas_byok.py
```

另：admin `frontend/admin/src/pages/LlmProviders.test.tsx`（73.4「点」；不得当 73.5–73.12 完成态）。
