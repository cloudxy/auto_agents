# T-10 · 出站钥匙必须绑恰好一家企业

> Wave 0｜角色：`/backend`｜粗估：1d｜状态：**done**｜contract **v1.6**
> 上游：`01-define/spec.md` FR-13 · contract §7.6

## FR 锚

| FR | GWT | 覆盖 |
|---|---|---|
| FR-13 | GWT-13.1, 13.2, 13.3, 13.4 | 是 |

**本票做什么**：未绑定到单一企业的钥匙拉数 → 拒绝，响应 0 行。绑定后仍带不上候选。旧字符串列表钥匙视为未绑定。

**本票不做什么**：租户自助发钥匙 = FR-51，本波不做。

## 依赖

| 前置票 | 需要它提供什么 |
|---|---|
| T-08 | 候选谓词 `source <> marketplace` 已在读模型 |

## expand-contract

旧字符串列表钥匙 **视为未绑定**（行为收紧，不是改列类型）。禁止一票把旧钥匙静默绑到某企业。

## GWT / Then（不得改写成短句）

- GWT-13.1：钥匙已绑定企业 A，用该钥匙拉 A 的非候选结果 → 只看到 A 的行。
- GWT-13.2：未配置任何绑定，任意钥匙拉数 → 拒绝，0 行。
- GWT-13.3：绑定 A 的钥匙指定 B 的爬虫名拉数 → 0 行 B 数据（跨租户可见事故红线）。
- GWT-13.4：旧的「字符串列表钥匙」尚未绑企业，使用视为未绑定，拒绝。

## 同 PR 测试

- 改 `backend/tests/test_external_api.py`（及出站拉数相关）：未绑定 0 行；跨企业 0 行 B；旧字符串钥匙拒绝；绑定后 `source <> marketplace`。
- 闸：`uv run pytest -x -q backend/tests/test_external_api.py`。
