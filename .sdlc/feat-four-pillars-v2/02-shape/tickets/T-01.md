# T-01 · 官网卖点闭集；删虚构规模；定价主按钮分档

> Wave 0｜角色：`/frontend` official｜粗估：1–2d｜状态：**done**｜contract **v1.8**
> 上游：`01-define/spec.md` FR-01 / FR-02 / FR-05 · `02-shape/contract.md` §11
> 票闸：**TK-03** 收到 GWT-70.4（与 T-20 并收；T-16 禁止单独勾「是」）；**TK-08** 70.4 闸须首页/功能介绍/定价三个必须存在的 node；SH-01…18 保持 closed

## FR 锚

| FR | GWT | 覆盖 |
|---|---|---|
| FR-01 | GWT-01.1, 01.7, 01.8, 01.9, 01.10, 01.2, 01.11, 01.12, 01.3, 01.4, 01.5, 01.6 | 是 |
| FR-02 | GWT-02.1, 02.2, 02.3 | 是 |
| FR-05 | GWT-05.1, 05.2, 05.3 | 是（与 01.7 / 01.12 同一出口） |
| FR-70 | GWT-70.4 | **部分**（与 T-20 并收：官网文案无「直连平台网关 / 我的中转令牌」；T-16 禁止单独勾「是」） |

**本票做什么**：官网首页 / 功能介绍 / 定价只验收闭集 A；闭集 B 撤或预告，主按钮不得伪装立即开通。删 Hero 虚构规模。不选定 Q-VOICE 首句。

**本票不做什么**：不代选 Q-VOICE / Q-PRICE / Q-BILL；不履约工单/渠道组/私有技能库/xlsx；不新建聊天 UI；不重排后台五组。

## 依赖

| 前置票 | 需要它提供什么 |
|---|---|
| — | 无 |

**并行**：∥ T-02 / T-03 / T-04 / T-06 / T-11。定价「无渠道组当前可买」由本票 + T-20 回归。

## expand-contract

无破坏性数据变更。文案/按钮 href 一次改完即可。

## GWT / Then（不得改写成短句）

- GWT-01.1：定价免费档三条数字与用量页相同（并发 5 / 存储 10,000 / 月度 20 万 tokens），不以「预告」出现，且不链到无关出口。
- GWT-01.7 / GWT-05.1：免费档主按钮进入企业注册。
- GWT-01.8：首页或功能介绍写「粘贴链接 / 任务 / 结果」不以预告出现；工人不在线走 FR-18 空态，不算本卖点撒谎。
- GWT-01.9：当前可买格式只有 CSV 或 JSON，并可见单次最多 100 条；不把 Excel/xlsx 写成当前可买。
- GWT-01.10：首页或定价出现「成员管理」或「用量看板」时，租户负责人登录后台能打开对应页并完成查看（不是预告）。
- GWT-01.2：工单支持 / 中转站渠道组分配 / 私有技能库 / 专属客户成功要么不出现，要么带「预告」且该项所在档的主按钮不是免费注册。
- GWT-01.11 / GWT-03.5：不把 Excel/xlsx 写成当前可买。
- GWT-01.12 / GWT-05.2：Q-PRICE / Q-BILL 未关闭时，专业档或企业档价签与主按钮不得写成「现在就能买到并开通」；主按钮不到企业注册。
- GWT-01.3：已登录租户看官网，「当前可买」集合与匿名访客相同。
- GWT-01.4：首页技能精选所依赖的公开列表不可用时，该区块显示「暂时无法加载能力」+「重试」，**禁止**空白或「还没有技能 / 暂无已发布」。
- GWT-01.5：租户公司管理员没有「编辑官网卖点 / 改定价文案」入口。
- GWT-01.6：不出现抽取准确率、AI 评分已校准、插件官方认证、渠道正品保证。
- GWT-02.1：首页 Hero 及首屏不出现 `128,000+` / `12 节点` / `3.2 亿条` 或同等绝对规模数字。脚注「示意数据」**不等于**允许保留数字。
- GWT-02.2：无足够真实聚合时不渲染占位大数；可以没有统计区。
- GWT-02.3：租户经办打开官网同样看不到虚构规模。
- GWT-05.3：已登录租户点专业档主按钮仍不到「再开一家免费企业」。
- **GWT-70.4**（本票与 T-20 并收）：已登录经办在浏览器寻找「直连平台网关 / 我的中转令牌」→ 无直连平台网关、无我的中转令牌。本票负责官网首页 / 功能介绍 / 定价文案与控件。不得与 GWT-72.3 / GWT-60.1 写成「同一句」。T-16 禁止单独勾「是」。

## 同 PR 测试

- 改 `frontend/official/src/pages/Home.tsx`（删 `HERO_STATS`：`128,000+` / `12 节点` / `3.2 亿条`）。
- 改 `frontend/official/src/components/home/FeaturesSection.tsx`（去掉「CSV / Excel 一键导出」当前可买；可写 CSV 或 JSON + 单次最多 100 条）。
- 改 `frontend/official/src/pages/Pricing.tsx`：免费档主按钮 `/register`；专业/企业主按钮 **不得** `href: '/register'`（mailto 或说明页或预告不可购买）；B1–B4 撤或标预告；B6 ¥299/定制不得写成当前可买。
- 改 `frontend/official/src/components/home/SkillsSection.tsx`（或等价精选块）：列表失败 Then = 「暂时无法加载能力」+「重试」。
- 同 PR 官网单测：断言 Hero 无上述三数；免费档 CTA → 注册；专业/企业 CTA 不到 `/register`；失败精选非空成功句。
- **GWT-70.4（TK-08）**：三个必须存在的 node id（文案与控件无「直连平台网关」、无「我的中转令牌」）。**未加这三个 node 之前不得勾 T-01 侧 70.4。** 禁止只跑 `Pricing.test` 勾 70.4。不得与 72.3「我的渠道组 / 定价 B2」写成同一句。
  1. `frontend/official/src/pages/Home.test.tsx::test_no_direct_gateway_or_relay_token_copy`
  2. `frontend/official/src/components/home/FeaturesSection.test.tsx::test_no_direct_gateway_or_relay_token_copy`
  3. `frontend/official/src/pages/Pricing.test.tsx::test_no_direct_gateway_or_relay_token_copy`
- 闸（三个 path 各跑；任一文件/node 未加必须红；禁止只跑 `Pricing.test`）：

```bash
npm run build --prefix frontend/official
npm test --prefix frontend/official -- --testPathPattern=Home.test --testNamePattern=test_no_direct_gateway_or_relay_token_copy
npm test --prefix frontend/official -- --testPathPattern=FeaturesSection.test --testNamePattern=test_no_direct_gateway_or_relay_token_copy
npm test --prefix frontend/official -- --testPathPattern=Pricing.test --testNamePattern=test_no_direct_gateway_or_relay_token_copy
```

