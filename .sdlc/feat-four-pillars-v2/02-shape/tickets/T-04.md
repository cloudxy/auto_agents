# T-04 · 平台写面 `require_platform_admin`；目录豁免；超管刷新可见

> Wave 0｜角色：`/backend`｜粗估：2d｜状态：**done**｜contract **v1.6**
> 上游：`01-define/spec.md` FR-06 / FR-20 · ADR-0017 · PIT-2 / PIT-3

## FR 锚

| FR | GWT | 覆盖 |
|---|---|---|
| FR-06 | GWT-06.1, 06.2, 06.3, 06.4, 06.6 | 是 |
| FR-06 | GWT-06.5 | **部分**：本票不把「经办写 `/llm`」当完成态；企业负责人保存本企业行的完整经办露出在 T-17（SH-09 / SH-11） |
| FR-20 | GWT-20.1, 20.2, 20.3 | 是 |

**本票做什么**：扫描插件/技能、验证插件、组建专家团、改渠道窗口、改平台级模型供应商、改系统配置、改平台角色权限：只有平台超管能做成。租户公司管理员（`admin` 但不是平台超管）失败时目录/额度/配置一行不变，并留下越权记录。平台目录 `tenant_id` 恒 NULL 行必须登记 `TENANT_EXEMPT_TABLES`，超管刷新看得见自己的改动。

**本票不做什么**：不改值班列表来源（T-18）；不把 `/llm` 本企业写与 test 改成 `require_operator`（T-17）；不新建聊天 UI。

## 依赖

| 前置票 | 需要它提供什么 |
|---|---|
| — | 无 |

**并行**：∥ T-01 / T-02 / T-03 / T-06 / T-11。T-05 依赖本票守卫已切。

## expand-contract

守卫切换必须与 b1c 测试 **同 PR**（PIT-2）。`capability_assets`（及本票碰到的恒 NULL 平台表）与 `TENANT_EXEMPT_TABLES` + 夹具 **同 PR**（PIT-3）。禁止先改守卫再下个 PR 补测试。

## GWT / Then（不得改写成短句）

- GWT-06.1：平台超管执行扫描或验证，操作完成；记录谁、对什么做了什么。
- GWT-06.2：可同步的包为 0 时超管扫描看到「没有可同步的包」类可行动空态，不是静默成功。
- GWT-06.3：租户公司管理员发起扫描或验证或改渠道窗口 → 被拒绝；目录/额度不变；留下越权记录。只读成员同样拒绝。
- GWT-06.4：仅登录的查看者发起扫描 → 拒绝且零落库（现网「查看者可扫」将被改写，不得再当验收金标）。
- GWT-06.5：**本票只验企业负责人保存本企业行可以保存、本企业行变更。禁止把 operator 403 写成 `/llm` 写面完成态**（经办写与 test 的 `require_operator` + `/llm` 露出在 T-17）。
- GWT-06.6：企业负责人激活或改 **平台级** 供应商 → 拒绝且平台行不变。
- GWT-20.1：超管扫描到新包，刷新目录，新包出现（不得静默 0 行）。
- GWT-20.2：目录本就为空，超管打开是可行动空态，不是加载失败装空。
- GWT-20.3：租户经办尝试改平台目录行 → 拒绝且行不变。

## 同 PR 测试

- 写面改 `Depends(require_platform_admin)` 的路由：扫描/验证/渠道窗口/平台级供应商/系统配置/平台角色权限。
- **同 PR 改** `backend/tests/test_b1c_capabilities_coverage.py`（作废 viewer 可扫 200 金标）；`test_b1c_newapi_channels_coverage.py`；相关 llm 平台行写面。
- `backend/app/tenant_isolation.py`：`capability_assets` 进 `TENANT_EXEMPT_TABLES`；`test_saas_isolation.py` 夹具同 PR（已登记豁免不注入 / 超管刷新可见）。
- 闸：`uv run pytest -x -q backend/tests/test_b1c_capabilities_coverage.py backend/tests/test_saas_isolation.py`；`bash scripts/check-arch.sh`。
