# T-08 · 候选不计配额、不进我的结果/导出/出站

> Wave 0｜角色：`/backend`｜粗估：1d｜状态：**done**｜contract **v1.6**
> 上游：`01-define/spec.md` FR-11 · ADR-0013

## FR 锚

| FR | GWT | 覆盖 |
|---|---|---|
| FR-11 | GWT-11.1, 11.2, 11.3 | 是 |

**本票做什么**：候选（`spider_results.source=marketplace`）不占企业结果配额，不出现在数据中心/导出/「我的结果」，出站拉数也带不上。超管审核列表能翻页看到候选。谓词 `source <> marketplace`。超管候选 SQL 分页。

**本票不做什么**：不把候选迁出结果表；不放宽 `tenant_id` 可空。

## 依赖

| 前置票 | 需要它提供什么 |
|---|---|
| T-07 | 入队/回流已带企业；无主不写结果 |

T-10 依赖本票排除谓词。

## expand-contract

无迁表。读模型加谓词，不是改 source 枚举。超管候选列表分页必须 SQL 侧，禁止先拉全表再内存滤当完成态。

## GWT / Then（不得改写成短句）

- GWT-11.1：企业已用存储配额 90%，另有候选行，经办再跑普通采集且未超自己的非候选条数上限 → 不被候选顶满而拒绝。
- GWT-11.2：只有候选、没有自己的条，打开数据中心按「还没有采集结果」而不是列出候选。
- GWT-11.3：租户经办打开候选审核 → 没有入口或与不存在同形；审核仅超管。

## 同 PR 测试

- 配额计数、我的结果、导出、出站统一 `source <> marketplace`。
- 改 `backend/tests/test_skill_candidates.py` / `test_spider_datacenter_crud.py`：候选不进租户列表；超管候选 SQL 分页。
- 闸：`uv run pytest -x -q backend/tests/test_skill_candidates.py backend/tests/test_spider_datacenter_crud.py`。
