# T-11 · 密钥离树：含 `deploy/litellm/config.gen.yaml`；ignore + 扫描 0 命中

> Wave 0｜角色：`/sre` + `/backend`｜粗估：1d｜状态：**todo**｜contract **v1.6**
> 上游：`01-define/spec.md` FR-14 · NFR-04

## FR 锚

| FR | GWT | 覆盖 |
|---|---|---|
| FR-14 | GWT-14.1, 14.2, 14.3, 14.4, 14.5 | 是 |

**本票做什么**：发布物与 git 跟踪文件不得含明文上游 Key。已跟踪的网关生成配置（含 `deploy/litellm/config.gen.yaml`）移出 git。ignore + 扫描 0 命中。轮换证据在交付清单，**正文不写密钥**。管理详情对租户不展示本机路径。租户若在仍能打开的设置里看到密钥字段：仅掩码。

**本票不做什么**：**不**启网关、**不**改 `llm_chat` 出口、**不**把网关写进根编排、**不**把 new-api 当运行时打开。运行时注入验收见 T-14 / FR-75。渠道页对租户不渲染密钥——因为租户按 GWT-07.3 根本看不到该页（T-05）。**禁止用「打开渠道页见掩码」当本 FR 的 Then。**

## 依赖

| 前置票 | 需要它提供什么 |
|---|---|
| — | 无 |

T-14 依赖本票离树已完成。

## expand-contract

先 ignore + 停止跟踪生成配置，再轮换（证据只在交付清单）。禁止在票正文/commit message 写密钥。filter-repo 清 git 史是操作者机外动作，本票清单要 evidence，正文不写密钥。

## GWT / Then（不得改写成短句）

- GWT-14.1：超管打开管理详情查看能力，不把本机绝对路径当作租户可复制字段发出给非超管。
- GWT-14.2：租户打开管理详情（若允许读）无本机路径。
- GWT-14.3：租户直打中转页走 GWT-07.3，页上无密钥。
- GWT-14.4：租户打开自己仍能进的系统设置且其中有密钥字段，查看仅掩码。
- GWT-14.5：仓库跟踪的配置样例检查发布物无真实上游 Key 明文（轮换证据在交付清单，不在本 GWT 写密钥）。含 `deploy/litellm/config.gen.yaml` 不在跟踪树。

## 同 PR 测试

- `.gitignore` 覆盖生成配置；`git ls-files` 对 `deploy/litellm/config.gen.yaml` 0 命中。
- CI/`git grep` 扫描跟踪树明文 Key 样例模式 0 命中（不把真密钥写进测试）。
- 租户设置密钥字段掩码测试（现有 `test_llm_provider.py` mask 合同同 PR 保持）。
- 闸：`git ls-files deploy/litellm/config.gen.yaml` 空；`bash scripts/check-arch.sh`。
