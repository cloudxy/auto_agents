# designer memory · feat-four-pillars-v2

> Facts this role learned (**what**). Procedures stay in SKILL.md (**how**).
> Cap **2200 characters**. Frozen snapshot: next spawn sees this; do not treat it as live mid-turn.

## Last

- Date: 2026-09-08
- Hat: 塑形 · UI 合同（UI-01 only; G-fresh r1 FAIL 的 copy 闸）
- Outputs:
  - `.sdlc/feat-four-pillars-v2/02-shape/flows/llm-test-connection.md`
  - `.sdlc/feat-four-pillars-v2/02-shape/edge-states.md`（未改；锁句原样）
  - `design-brief.md` 未改（无短句重复）

## Facts

- Live 合同在 v2 `02-shape/`，禁止抄 `.sdlc/feat-four-pillars/02-shape/`。
- 后台五组冻结；叶「资产目录」→「能力市场」；组内加 tenantOnly「我的安装」。
- GWT-73.4：经办进 `/llm`（无 requireAdmin）+ 有 `menu:llm` 无 `menu:newapi` + **点**该行「测试连接」；测该行地址；HTTP 200 无点击不满足。
- **UI-01 closed：** 73.4 失败句全特征只锁 edge-states 一句：「连接失败：连的是本企业供应商「{name}」，不是平台网关。{原因或「该行地址无响应」}。检查该行地址与密钥后，再点测试连接。」总览短句已删。禁止 71.2 / 74.1 / 「暂无渠道」。离线句（edge-states `/llm` 离线）仍是另一态，不是第三句失败 copy。
- 71.2：「还没有平台模型，去网关登记」。71.3：「LLM 网关管理面不可达，仅本地事件/探针」。禁「暂无渠道」。
- 无 `/chat`。similar-suggest 非产品屏。
- 官网 404：「页面不存在，可能已被移除或地址有误」+「返回首页」。semantic.primary `#0958d9`。

## Open (mine)

- 满额 mailto 收件人 → 配置 `CONTACT_MAIL`，不代选 Q-BILL。
- 71.2 不外链 LiteLLM Admin UI。

## Do not re-litigate

- Q-VOICE/PRICE/RELAY/MARKET-USER/BILL/AGPL
- D1–D29；不礼包；unlist≠停用；七叶；无四柱 Hero；无 enable-host
- 每屏六态含 permission/offline；失败≠空
- SH-01…18 / TK-01…08；不重排五组；不加聊天 UI
