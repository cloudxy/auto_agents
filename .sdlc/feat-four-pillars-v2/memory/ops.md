# ops memory · feat-four-pillars-v2

> Facts this role learned (**what**). Procedures stay in SKILL.md (**how**).
> Cap **2200 characters**. Frozen snapshot: next spawn sees this; do not treat it as live mid-turn.

## Last

- Date: 2026-09-08
- Hat: sdlc-workflow:ops（定义帽诊断）
- Outputs: `.sdlc/feat-four-pillars-v2/01-define/diagnosis/ops.md`

## Facts

- 不是 P9。v2 `hats_done: []`；旧 `feat-four-pillars` superseded、G-fresh 未过、Wave 0/1 未上线。旧 spec/contract = 输入，不是现行合同。
- 客户工单/访谈/NPS/产品事件 SDK = 0。`.scratch/` 不存在。影响面一律未量化。
- 官网履约相对 09-07 **未变**：采集 Slogan、HERO_STATS 示意、Excel 文案 vs 拒 xlsx、双广场、定价三档都进 `/register`、注册成功「再注册一家」、无去登录。
- 已兑：免费档 5/10000/200000 = `DEFAULT_QUOTA`。
- CONTEXT 已五类平级 + 订插件不带礼包 + 上架≠停用 + 上架独立闸。访客面仍四 Tab「专家」；Admin 仍「验证后方可分发」。
- `POWER_MARKET`/installs 代码 0；`listing_state` 只在 CONTEXT。NEWAPI 默认 false。
- 蓝图事件名产品代码 0 命中。WACT 仍不能报。QA-02 **fixed**（不要再当 blocker）；不能判定的原因是查询面未建。
- 七问仍开（含 Q-AGPL）。定义 T-xx ≠ 塑形 T-xx ≠ v2 票。
- 竞品 F-01…F-R2 仍真；未试用。必须吸收 M-01…M-15（诚实履约、五动词、五类词、D20 运营 unlist、禁止双北极星）。
- Pitfalls 仍有效：P-OPS-01 Excel/xlsx；P-OPS-02 双公开闸；P-OPS-03 勿在查询面未建时报 WACT。

## Open (mine)

- 七问 ops 不代答。
- Wave 1 若做商店：unlist/许可 override 值班人未指定（S-V2-04）。
- v2 是否原样冻结旧蓝图事件名 → `/pm`。
- 下轮是否主动收集（注册一题）→ `/pm`；样本可能仍 0。

## Do not re-litigate

- appetite 8h 覆盖四柱（已否）。
- CONTEXT 仍四类 / QA-02 仍 open / Q-AGPL 未进 §9.1。
- 「用户强烈要求安装按钮 / Actor 店 / Power Market」。
- 用 Hero/成功率当增长；把旧 T-xx 当 v2 工单。
- 代选 Q-VOICE 首屏句。
