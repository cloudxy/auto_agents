# Briefing · 合入后下一波（post-merge-upgrade）

- 表面：ToB 先（采集经办 / 租户管理员 / 平台管理员 / 买方）；官网商店面随 Power Market，**不做 ToC 增长环**
- Appetite：端态是四角色功能完备；**施工分波**，本窗链帽执行、不等「继续」。单波超预算 50% 停下重判。禁止把采集出数与 live 支付挤进同一 2 人周
- Claim：**已经合入 main × LiteLLM L1 的半成品**，在「示意完整、双轨并存、环境闸未跑」的现状里，按角色把可判定缺口补齐到完备，并把六个开放问一次关死；不是再开「实现四支柱」功能并集巨票
- 泳道：L3（租户边界 + 支付/网关外部合同 + 不可逆收款）
- 分类：**product-change**
- 操作者 2026-09-13 Discuss-P 原话：Q1 冲突由 AI 判断缺哪块更好然后执行；Q2 经办功能必须完备全面；Q3 买方必须功能做完；Q4 租户管理员所有功能做完；Q5 平台管理员所有功能做完；Q6 六个开放问一次全部处理完

## Discuss-P

**谁 / 现在 / 期望 / 凭什么说改善了**

操作者本轮没有答「今天怎么做 / 卡在哪」的过程句，直接给端态。三连问用调研 E2 补齐过程，端态以操作者拍板为准（E1）。

| 角色 | 今天怎么做（E2 调研） | 卡在哪（E2） | 下一步他们会怎样（E2） | 操作者本轮端态（E1） |
|---|---|---|---|---|
| 买方 | 官网「去结账」与用量页「套餐与订购」都能点 | 双轨；live 支付未通；0 笔生产支付 | 继续免费或找超管点确认 | **开通负责人必须功能做完**（一条能走完的结账/开通故事） |
| 使用者（采集经办） | 贴 URL / 任务 / AI 规划 / 数据中心 | 工人离线 vs 配额 vs LLM 默认关；C4 未跑 | 手配或只用本机 | **功能必须完备全面**（第一次出数可判定，工厂叶可走完） |
| 租户管理员 | 成员 / 用量 / 出站钥匙 / 渠道组 / 我的安装 | 钥匙三环 + 两套产品名；CONTEXT 词表滞后 | 记错钥匙或不用平台拉数 | **租户管理员所有功能做完**（成员、用量、钥匙、渠道组、安装各一条故事） |
| 平台管理员 | `/newapi` 三问驾驶舱 + 运营台 + 用户 + 偶发 `/litellm/keys` | 值班双入口；C2 真网关轮未跑；签发 ≠ live | 另开网关或 BYOK | **平台管理员所有功能做完**（值班单一入口 + 运营台 + 用户/设置） |

买方 vs 使用者：买方要能开通/付钱；经办要贴 URL 出数。**Q-VOICE 已关=采集** → 北极星仍是合格出数；结账是驱动，不改 Hero。

四面都点名。无租户访谈原声。

## Discuss-S

探针卡（ToB）：租户边界要；RBAC 三态要；SSO 本波 N/A；套餐硬闸保留；审计保留；空租户第一次成功=采集出数；SLA 本波 N/A；webhook 保留 L1 交付回调。ToC 增长环 **N/A**（Q-C-REG=不开个人空间）。

| 方案 | 内容 | 裁决 |
|---|---|---|
| A. 只收双轨 + 跑环境闸 | 结账一条、钥匙一条、值班一条；不宣称角色完备 | **否。** 小于操作者端态 |
| **B. 端态完备、施工分波** | 四角色功能完备是**验收端态**；定义帽按依赖切波写 FR。波序由 AI 按「缺了就卡住主路径」排序：经办出数 → 买方一条结账 → 租户钥匙/成员/用量 → 超管值班单一入口 → 市场订一行（flag 可开）。本窗链帽、不等继续 | **采纳（操作者 Q1：冲突由 AI 判更好的缺块然后执行）** |
| C. 同一 2 人周做完所有角色+live 支付+开「当前可买」 | 四家竞品并集 + 文案 GA | **否。** 杀死条件：未量化不得 pass；不得把有条件放行写成四柱 GA；live 支付与采集不得挤同一 2 人周 |

**AI 对 Q1 的模块优先级（缺了就更好先做）：**

1. 采集经办第一次成功（Hero 已锁采集；无出数则其余柱无法演示）
2. 买方单一结账故事（拆用量页 BillingPanel 与 Checkout 双轨；必须功能=能下单、能看见状态、能被开通，不要求本波 live 沙箱）
3. 租户管理员：出站钥匙单一产品名 + 成员/用量/渠道组可走完（ADR-0020 分平面保留，三环查找收口）
4. 平台管理员：值班与 LiteLLM admin 合成操作者单一入口；运营台/用户/设置可走完；C2 列为环境闸不是代码 FR 的替代
5. 能力市场：flag 打开后能订一行；关时诚实未开放

范围外仍有效：开发者门户、分成、Casbin、Stripe/兑换码/Discord、把 new-api 请回运行时、数仓、专家团执行、LiteLLM Admin UI 租户化、个人 ToC 空间。

## Compete

**压缩。** 长文：`00-discover/compete.md`。

集合：LiteLLM/NewAPI（中转）· Dify/Coze（市场）· Crawlab/Apify（采集）· **现状=合入后本仓半成品（必填）**。

本变更一行：收双轨、四角色走完可判定路径；不并集竞品店。

结论：**差异化**四柱同租户壳 + 出站≠`sk-` 分平面 + 404 同形 + 禁「当前可买」直到 live。借鉴「一条对外故事」。回避充值码/Stripe/Admin UI 租户化/Casbin。来源 E2。

## Market

**压缩。** 长文：`00-discover/market.md`。

单位：ToB 租户/买方。下界/上界：**未量化**。0 工单 / 0 UV / 0 live 支付。本机 4 行租户不是买方。`POWER_MARKET`/`LITELLM`/`LLM` 默认关。

给 RICE 的 Reach：**未量化**。不打分。

## Falsify

杀死条件（预先写死，操作者「全部做完」**不**放松）：

- 未量化不得 **通过（pass）**
- 不得把有条件放行写成四柱 GA
- live 通道指纹未跑不得对外印「当前可买」
- 不得为 appetite 砍租户隔离 / 404 同形 / 密钥不进 git
- 采集出数与 live 支付不得写成同一 2 人周

| ID | 假设（可证伪的一句） | 若假则 | 最便宜检验 | 证据级 | 结果 |
|---|---|---|---|---|---|
| A1 | 存在一批买方租户每周会用齐四角色功能 | 本波只服务操作者演示 | 0 工单 / 0 UV / 0 支付（market.md） | E2 空仓 | **杀死「市场 pass」** |
| A2 | 「全部做完」可以在同一 2 人周交付且含 live 支付 | 必须分波；支付与采集拆开 | 上带 appetite 与 compete「一条故事」 | E2 | **杀死方案 C** |
| A3 | 经办第一次出数仍是卡住主路径的最大缺口 | 应先做结账或市场 | Hero 锁采集；C4 未跑；LLM 默认关 | E2 | **存活**（故波序 1=经办） |
| A4 | 买方必须功能=可见一条结账故事，不是再接一家支付 | 否则要接 live 沙箱才能定义 | 双轨代码 E2；0 live 支付 | E2 | **存活** |
| A5 | 出站 vs `sk-` 分平面是护城河，三环查找才是债 | 应合并成一把 sk- | ADR-0020；compete 差异化 | E2 | **杀死「统一成一把 sk-」** |

本轮裁决：**缩小后存活 + 当作赌注**（不得标通过）

- **缩小后的 Claim：** 端态=四角色功能完备；本定义波把可判定缺口写成 FR 并按上面 1→5 排序。不宣称 GA、不印「当前可买」直到 live 指纹。
- **赌注：** Confidence ≤50%。复盘指标 = **非夹具租户、一次合格采集完成且条数>0**（Q-VOICE=采集）。驱动指标 = **买方只看见一条结账故事且能走到待支付/已开通终态**（不把 live 沙箱当北极星）。

## Glossary

将升到 CONTEXT.md（define 后由实现帽改词表，发现带只点名）：

- 计费：在线结账走通道通知（ADR-0024）；线下确认走 `confirm_paid`；租户可见面只保留一条故事
- 中转令牌：租户渠道组 `sk-` 已发；平台 LiteLLM admin keys 是操作者面，不是第二套租户店
- 出站钥匙：拉数平面；不得与 `sk-` 混名
- 当前可买：营销四字；未有 live 支付指纹前禁止

## Fog / 范围外

开发者门户、分成、Casbin、Stripe、new-api 运行时、数仓、专家团执行、LiteLLM `/ui` 租户化、个人注册空间、把 HMAC 夹具写成已通支付。

## Q-*（操作者：一次全部处理完 → 本窗 AI 关死，不再待确认）

| id | 裁决 | 依据 | 状态 |
|---|---|---|---|
| Q-VOICE | **对外第一句=采集** | Hero 已锁；上带已答；北极星=出数 | 已答 |
| Q-PRICE | **专业档保持 ¥299**；确认收款/在线履约后套用该档三数字配额；渠道组是企业档履约能力不是免费赠品；租户只走一条结账故事 | 定价页 E2；双轨是债不是两个价 | 已答 |
| Q-MARKET-USER | **主用户=平台治理上架 + 租户按一行订阅**；侧栏落点=能力市场货架 / 我的安装；不做作者投稿门户 | D1–D29；compete 回避开发者门户 | 已答 |
| Q-AGPL | **运行时锁定 LiteLLM，New API 退役**。中转按企业档 SKU **可施工可结账**。对外四字「当前可买」**等 live 通道指纹（C2）后再开文案** | New API AGPL 墓碑 E2；LiteLLM 非发卡网 | 已答 |
| Q-OPS-COLLECT | **值班通道=超管 `/newapi` 三问驾驶舱（本波收成单一操作者入口）**；对外联系=`OPS.DUTY_CONTACT`，空则不展示联系 CTA | 无 Contact 页 E2；mailto 是占位 | 已答 |
| Q-C-REG | **不开 C 端个人空间**；注册=创建企业 | ToB 先；注册页已强制企业名 | 已答 |
