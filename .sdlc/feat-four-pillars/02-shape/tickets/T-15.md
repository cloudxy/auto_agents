# T-15 · B4 域 import 闸 + R10 扫子包

> 泳道：L4｜角色：`/backend`｜粗估：0.5d｜状态：**todo**
> 上游：ADR-0010；NFR-10；`scripts/check-arch.sh`

## FR 锚点

| FR | GWT | 本票是否完整覆盖 |
|---|---|---|
| NFR-10 | 配置与密钥不进代码；市场包边界 | 配套（不是用户 GWT） |

## 边界

**本票做什么**：`scripts/check-arch.sh` 增加 B4：`backend/services/power_market/` 禁止 import `spider_*` / `newapi_*` / `ai_planner` / `channel_*`。包目录可先建空 `__init__.py` 以免规则空转。R10 日志扫描扩到 `backend/services/**/*.py`。挂现有 lint 闸门（已是 check-arch.sh）。

**本票不做什么**：实现适配器（T-19+）；一次性搬 domains/。

**会改哪些文件**：

| 文件 | 改动性质 |
|---|---|
| `scripts/check-arch.sh` | 修改：B4 |
| `backend/services/power_market/__init__.py` | 新增：空包 |
| 可选 B4 单测/注释 | 新增 |

**不许改的文件**：业务服务实现

## 依赖

无。

## 验收清单

- [ ] `bash scripts/check-arch.sh` 在当前树 exit 0
- [ ] 故意在 `power_market` 写 `from backend.services.spider_task_service import X` 时 B4 红
- [ ] 空包不引入新运行时行为

## 自测命令

```bash
bash scripts/check-arch.sh
```

## 进度记录

| 时间 | 动作 | 说明 |
|---|---|---|
| | | |

## Evidence（交票前必填）

```
$ <命令>
<输出>
exit: 0
```

## 交票自检

- [ ] 每条验收项有 evidence（命令 + 退出码原样）
- [ ] 自测全绿
- [ ] 只改了「会改哪些文件」里列出的文件
- [ ] 未触碰「不许改的文件」
- [ ] 发现的上游问题已回报，不是自行绕过
- [ ] 状态已更新为 done
