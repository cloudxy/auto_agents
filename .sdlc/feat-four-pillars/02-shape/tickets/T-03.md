# T-03 · 中转站与平台 LLM 写面收权

> 泳道：L4｜角色：`/backend`｜粗估：1d｜状态：**todo**
> 上游：PRD FR-07；ADR-0017；`contracts/auth-guards.md`

## FR 锚点

| FR | GWT | 本票是否完整覆盖 |
|---|---|---|
| FR-07 | GWT-07.1, 07.2, 07.4 | 是（07.3 导航/404 在 T-04） |

## 边界

**本票做什么**：渠道窗口写、平台 LLM 供应商写（含激活平台行）改为 `require_platform_admin`。租户自有 LLM 行保持租户经理可写。拒绝时额度/供应商不变 + 审计。管理面不可达时提交失败不改原值。

**本票不做什么**：前端隐藏/404（T-04）；探针熔断策略（FR-61）；租户渠道组屏（Q-RELAY）。

**会改哪些文件**：

| 文件 | 改动性质 |
|---|---|
| `backend/app/api/v1/newapi.py` | 修改：写 Depends |
| `backend/app/api/v1/llm_providers.py` | 修改：按平台行 vs 租户行分流守卫 |
| `backend/tests/test_newapi_api.py` 等 | 修改：租户 admin 不再当超管 |

**不许改的文件**：`NewapiApiClient` 探针逻辑；`capabilities.py`（T-02）

## 依赖

无（可与 T-02 并行）。

## 验收清单

- [ ] GWT-07.1：超管改窗口额度或激活平台 LLM 供应商 → 生效 + 审计
- [ ] GWT-07.2：管理面不可达 → 降级说明；提交失败额度保持原值
- [ ] GWT-07.4：租户公司管理员提交改额度或改平台供应商 → 拒绝；原值不变；审计越权。只读/经办同样
- [ ] 租户 BYOK 自有行仍可被该租户经理更新（回归）

## 自测命令

```bash
uv run pytest -x -q backend/tests/test_newapi_api.py backend/tests/test_saas_byok.py
bash scripts/check-arch.sh
```

## 进度记录

| 时间 | 动作 | 说明 |
|---|---|---|
| | | |

## Evidence（交票前必填）

```
$ <命令>
<输出>
exit: 0
```

## 交票自检

- [ ] 每条验收项有 evidence（命令 + 退出码原样）
- [ ] 自测全绿
- [ ] 只改了「会改哪些文件」里列出的文件
- [ ] 未触碰「不许改的文件」
- [ ] 发现的上游问题已回报，不是自行绕过
- [ ] 状态已更新为 done
