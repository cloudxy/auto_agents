# T-04 · Admin 平台页按 is_platform_admin；渠道页对租户同 404

> 泳道：L4｜角色：`/frontend`｜粗估：1.5d｜状态：**todo**
> 上游：PRD FR-06 FR-07 GWT-07.3；`contracts/auth-guards.md`；designer 诊断（本程序不改菜单分组）

## FR 锚点

| FR | GWT | 本票是否完整覆盖 |
|---|---|---|
| FR-06 | GWT-06.3 按钮级隐藏 | 部分（后端已拒；本票藏扫描/验证） |
| FR-07 | GWT-07.3 | 是 |

## 边界

**本票做什么**：登录投影 `is_platform_admin`。`ProtectedRoute` 不再把租户 `role=admin` 当平台超管。渠道/中转：导航对租户不出现；直打 URL 渲染与 NotFound **相同**（不是 Unauthorized 道歉页）。插件扫描/验证按钮 `usePermission` 或超管条件隐藏。`/llm` 对租户保留自有供应商，隐藏平台行写控件。

**本票不做什么**：菜单五组重组、幽灵 `/enterprise` `/rbac`（spec T-31）；商店面（Wave 1）。

**会改哪些文件**：

| 文件 | 改动性质 |
|---|---|
| `frontend/admin/src/services/auth.ts` | 修改：`is_platform_admin` |
| `frontend/admin/src/components/ProtectedRoute.tsx` | 修改 |
| `frontend/admin/src/config/menuConfig.tsx` | 修改：中转叶对租户隐藏（不改分组） |
| `frontend/admin/src/App.tsx` | 修改：`/newapi` 非超管走 404 |
| `frontend/admin/src/pages/Capabilities.tsx` | 修改：验证/扫描权限 |
| `frontend/admin/src/pages/NewApiOps.tsx` / LLM 页 | 修改：写控件 |

**不许改的文件**：`frontend/official`（T-09）；后端守卫（T-02/T-03）

## 依赖

| 前置票 | 需要它提供什么 |
|---|---|
| T-02 T-03 | 后端 403 合同；登录字段 |

## 验收清单

- [ ] GWT-07.3：租户公司管理员导航无渠道/中转。直打结果与页面不存在相同：无渠道数据、非道歉 403。只读/经办同样
- [ ] GWT-06.3：租户看不到扫描/验证主按钮（发现性动作禁用+说明或隐藏）
- [ ] 超管仍能进渠道页与治理扫描

## 自测命令

```bash
npm test --prefix frontend/admin -- --watchAll=false
npm run build --prefix frontend/admin
```

## 进度记录

| 时间 | 动作 | 说明 |
|---|---|---|
| | | |

## Evidence（交票前必填）

```
$ <命令>
<输出>
exit: 0
```

## 交票自检

- [ ] 每条验收项有 evidence（命令 + 退出码原样）
- [ ] 自测全绿
- [ ] 只改了「会改哪些文件」里列出的文件
- [ ] 未触碰「不许改的文件」
- [ ] 发现的上游问题已回报，不是自行绕过
- [ ] 状态已更新为 done
