# T-01 · 平台目录豁免登记（capability_*）

> 泳道：L4｜角色：`/dba`｜粗估：0.5d｜状态：**todo**
> 上游工件（必读，不要凭票面猜）：
> - PRD：`.sdlc/feat-four-pillars/01-define/spec.md` FR-06
> - 方案：`02-shape/contract.md` §7、ADR-0015
> - 契约：`02-shape/contracts/auth-guards.md`

## FR 锚点

| FR | GWT | 本票是否完整覆盖 |
|---|---|---|
| FR-06 | GWT-06.1（平台态能写目录）配套数据不变量 | 部分（守卫在 T-02；本票只修 0 行命中） |

## 边界

**本票做什么**：把平台目录表登记进 `TENANT_EXEMPT_TABLES`，使租户态 Core UPDATE 不再把恒 NULL 的平台行滤成 0 行。同步修正清单注释（「唯一功能必需的豁免是 skills」已过时）。

**本票不做什么**：

- 不改 API 守卫（T-02）
- 不建市场新表（Wave 1 T-18）
- 不给 CapabilityAsset 加 TenantMixin
- 不豁免未来的 `capability_installs`

**会改哪些文件**：

| 文件 | 改动性质 |
|---|---|
| `backend/app/tenant_isolation.py` | 修改：登记 `capability_assets` / `capability_plugins` / `capability_experts` / `capability_commands` / `capability_teams` |
| `backend/tests/test_saas_isolation.py` 或新测 | 修改/新增：豁免夹具 |

**不许改的文件**：`platform_core/tenant_context.py`（B1 不写业务表名）；Alembic 迁移（本票无 schema）

## 依赖

无。

**并行安全性**：与 T-03/T-05 不改同一文件 ☑

## 验收清单

- [ ] GWT-06.1 配套：平台态（`platform_scope`）对 `capability_assets` 的 Core UPDATE/双写 flush 命中 >0（不再被注入 `tenant_id=?`）
- [ ] 租户态 SELECT 目录仍可见平台行（非 Mixin，不过滤——符合平台目录）
- [ ] R13：`bash scripts/check-arch.sh` 豁免清单与组装点仍自洽
- [ ] **禁止** 把 `capability_installs` 写入清单（表本波甚至还不存在；注释写明禁止）

## 自测命令

```bash
uv run pytest -x -q backend/tests/test_saas_isolation.py backend/tests/test_saas_r13_overwrite.py
bash scripts/check-arch.sh
```

## 进度记录

| 时间 | 动作 | 说明 |
|---|---|---|
| | | |

## Evidence（交票前必填）

```
$ <命令>
<输出>
exit: 0
```

## 交票自检

- [ ] 每条验收项有 evidence（命令 + 退出码原样）
- [ ] 自测全绿
- [ ] 只改了「会改哪些文件」里列出的文件
- [ ] 未触碰「不许改的文件」
- [ ] 发现的上游问题已回报，不是自行绕过
- [ ] 状态已更新为 done
