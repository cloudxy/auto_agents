# T-02 · 能力/技能目录写面收权

> 泳道：L4｜角色：`/backend`｜粗估：1d｜状态：**todo**
> 上游：PRD FR-06；`contract.md` ADR-0017；`contracts/auth-guards.md`

## FR 锚点

| FR | GWT | 本票是否完整覆盖 |
|---|---|---|
| FR-06 | GWT-06.1, 06.2, 06.3 | 是（上架属 Wave 1；本票覆盖扫描/验证） |

## 边界

**本票做什么**：能力/技能目录写路径改为 `require_platform_admin`。拒绝时目录不变并记审计。空目录扫描给可行动空态，不装成「扫描完成」。**同 PR 改** `test_b1c`：viewer 扫描 403。

**本票不做什么**：中转/LLM 写（T-03）；前端按钮隐藏（T-04）；listing PATCH（Wave 1）。

**会改哪些文件**：

| 文件 | 改动性质 |
|---|---|
| `backend/app/api/v1/capabilities.py` | 修改 Depends |
| `backend/app/api/v1/skills.py` | 修改写面 Depends |
| `backend/tests/test_b1c_capabilities_coverage.py` | 修改合同 |
| 审计调用点 | 修改：越权也 `record_audit` |

**不许改的文件**：`deps.require_admin` 的其它消费者（T-03 分开）；前端

## 依赖

| 前置票 | 需要它提供什么 | 状态 |
|---|---|---|
| T-01 | 豁免后平台态能真正写目录 | todo |

**并行安全性**：与 T-03 不改同一路由文件 ☑

## 验收清单

- [ ] GWT-06.1：平台超管发起插件验证或目录扫描 → 执行；审计含操作者、动作、对象
- [ ] GWT-06.2：空目录扫描 → 「没有可同步的包」类空态，不是假成功
- [ ] GWT-06.3：租户公司管理员扫描/验证 → 拒绝；提示需要平台管理员；目录一行不变；审计越权
- [ ] viewer 同样 403（今日契约作废）

## 自测命令

```bash
uv run pytest -x -q backend/tests/test_b1c_capabilities_coverage.py backend/tests/test_skill_public_api.py
bash scripts/check-arch.sh
```

## 进度记录

| 时间 | 动作 | 说明 |
|---|---|---|
| | | |

## Evidence（交票前必填）

```
$ <命令>
<输出>
exit: 0
```

## 交票自检

- [ ] 每条验收项有 evidence（命令 + 退出码原样）
- [ ] 自测全绿
- [ ] 只改了「会改哪些文件」里列出的文件
- [ ] 未触碰「不许改的文件」
- [ ] 发现的上游问题已回报，不是自行绕过
- [ ] 状态已更新为 done
