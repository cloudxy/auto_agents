# T-16 · 出站 Key 无租户绑定则拒绝

> 泳道：L4｜角色：`/backend`｜粗估：0.5d｜状态：**todo**
> 上游：PRD GWT-08.4 FR-08；`contracts/outbound-data-api.md`

## FR 锚点

| FR | GWT | 本票是否完整覆盖 |
|---|---|---|
| FR-08 | GWT-08.4 | 是 |
| FR-52 | — | 否（明确不做租户自助钥匙） |

## 边界

**本票做什么**：`GET /external/v1/data/{spider_name}` 在 Key 命中但未绑定单一 `tenant_id` 时拒绝，响应不含任何企业行。绑定 Key 只返回该租户且排除 marketplace。空名单继续全拒。旧字符串列表视为未绑定 → 拒绝（expand：行为收紧，不是混拉）。

**本票不做什么**：租户自助创建钥匙（FR-52）；把现网钥匙写成可接受的跨租户后门。

**会改哪些文件**：

| 文件 | 改动性质 |
|---|---|
| `config/default/external_api.yml` | 修改：绑定形态注释（无明文 Key） |
| `backend/app/external_api/v1/public.py` | 修改 |
| `backend/services/spider_query_service.py` | 修改：query 带 tenant_id |
| `backend/tests/test_external_api.py` | 修改 |

**不许改的文件**：官网；内部 `/api/v1/spiders/results` 租户 JWT 路径

## 依赖

无。

## 验收清单

- [ ] GWT-08.4：钥匙未绑定单一企业 → 请求拒绝，响应不含 A 或 B 的行；不得混两家
- [ ] 空名单继续全部拒绝
- [ ] 绑定 tenant=A 时不含 B 的行、不含 marketplace

## 自测命令

```bash
uv run pytest -x -q backend/tests/test_external_api.py
bash scripts/check-arch.sh
```

## 进度记录

| 时间 | 动作 | 说明 |
|---|---|---|
| | | |

## Evidence（交票前必填）

```
$ <命令>
<输出>
exit: 0
```

## 交票自检

- [ ] 每条验收项有 evidence（命令 + 退出码原样）
- [ ] 自测全绿
- [ ] 只改了「会改哪些文件」里列出的文件
- [ ] 未触碰「不许改的文件」
- [ ] 发现的上游问题已回报，不是自行绕过
- [ ] 状态已更新为 done
