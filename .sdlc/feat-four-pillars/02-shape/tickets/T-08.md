# T-08 · 用量将满文案 + 上海业务日

> 泳道：L4｜角色：`/backend` `/frontend`｜粗估：1.5d｜状态：**todo**
> 上游：PRD FR-12 FR-16；`contracts/quota-and-isolation.md`；`contracts/conventions.md`

## FR 锚点

| FR | GWT | 本票是否完整覆盖 |
|---|---|---|
| FR-12 | GWT-12.1, 12.2, 12.3, 12.4 | 是 |
| FR-16 | GWT-16.1, 16.2, 16.3 | 是 |

## 边界

**本票做什么**：用量 API 给 percent/warn_level/timezone。前端删除 `429 QUOTA_EXCEEDED` 字样，按维度 CTA。仪表盘与用量同一 Asia/Shanghai 窗口并标明时区。月界用上海日历，不用 `datetime.utcnow()`。

**本票不做什么**：支付升级（Q-BILL）；超管用量页改配额冒充自助。

**会改哪些文件**：

| 文件 | 改动性质 |
|---|---|
| `backend/app/api/v1/tenant_usage.py` | 修改 |
| `backend/services/quota_service.py` | 修改：异常文案 |
| `frontend/admin/src/pages/Usage.tsx` | 修改 |
| `frontend/admin/src/pages/Dashboard.tsx` | 修改：窗口说明 |
| 时区单测 | 新增 freeze |

**不许改的文件**：官网 Pricing（T-09）

## 依赖

| 前置票 | 需要它提供什么 |
|---|---|
| T-06 | LLM 用量数字是真闸 |

## 验收清单

- [ ] GWT-12.1：70–89% 警告 + 维度名
- [ ] GWT-12.4：90–99% 接近上限 + 分流按钮
- [ ] GWT-12.2：满额中文下一步；无 `QUOTA_EXCEEDED` / `429` 字样
- [ ] GWT-12.3：超管无租户上下文不是「暂无用量数据」
- [ ] GWT-16.1：近 7 日与本月同时区并标明
- [ ] GWT-16.2：上海 00:30 落在新日历月
- [ ] GWT-16.3：超管「平台合计」vs 租户「本企业」

## 自测命令

```bash
uv run pytest -x -q backend/tests/test_saas_quota.py
npm test --prefix frontend/admin -- --watchAll=false
```

## 进度记录

| 时间 | 动作 | 说明 |
|---|---|---|
| | | |

## Evidence（交票前必填）

```
$ <命令>
<输出>
exit: 0
```

## 交票自检

- [ ] 每条验收项有 evidence（命令 + 退出码原样）
- [ ] 自测全绿
- [ ] 只改了「会改哪些文件」里列出的文件
- [ ] 未触碰「不许改的文件」
- [ ] 发现的上游问题已回报，不是自行绕过
- [ ] 状态已更新为 done
