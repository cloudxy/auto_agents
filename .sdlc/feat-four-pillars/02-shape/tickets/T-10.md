# T-10 · 导出格式/100 条上限可见

> 泳道：L4｜角色：`/frontend` `/backend`｜粗估：1d｜状态：**todo**
> 上游：PRD FR-03；`contracts/quota-and-isolation.md`

## FR 锚点

| FR | GWT | 本票是否完整覆盖 |
|---|---|---|
| FR-03 | GWT-03.1, 03.2, 03.3 | 是 |

## 边界

**本票做什么**：结果/数据中心导出选项只列 csv（及已实现的 json）。按钮旁写「单次最多 100 条」。后端导出硬顶 100；0 条不生成空文件。跨租户拒绝+审计。官网 Excel 卖点由 T-09 删。

**本票不做什么**：xlsx 实现（下一轮）；改 100 上限数字。

**会改哪些文件**：

| 文件 | 改动性质 |
|---|---|
| `backend/services/spider_query_service.py` | 修改：硬顶 + 0 条 |
| `frontend/admin/src/pages/Data.tsx` | 修改：文案（已有 100 则核对） |
| 任务结果抽屉 | 修改：无 xlsx |
| 测试 | 新增：100 界上、0 条、跨租户 |

**不许改的文件**：official Features（T-09）；候选过滤谓词（T-07 提供）

## 依赖

| 前置票 | 需要它提供什么 |
|---|---|
| T-07 | 导出不含 marketplace |

## 验收清单

- [ ] GWT-03.1：30 条 → CSV 30 行业务数据；无点了会失败的 Excel
- [ ] GWT-03.2：100 条得到 100 并写明上限；0 条提示不生成空文件
- [ ] GWT-03.3：A 导 B 不执行；审计越权

## 自测命令

```bash
uv run pytest -x -q backend/tests -k export
npm test --prefix frontend/admin -- --watchAll=false
```

## 进度记录

| 时间 | 动作 | 说明 |
|---|---|---|
| | | |

## Evidence（交票前必填）

```
$ <命令>
<输出>
exit: 0
```

## 交票自检

- [ ] 每条验收项有 evidence（命令 + 退出码原样）
- [ ] 自测全绿
- [ ] 只改了「会改哪些文件」里列出的文件
- [ ] 未触碰「不许改的文件」
- [ ] 发现的上游问题已回报，不是自行绕过
- [ ] 状态已更新为 done
