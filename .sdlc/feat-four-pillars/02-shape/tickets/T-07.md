# T-07 · 市场候选排除配额与租户结果

> 泳道：L4｜角色：`/backend`｜粗估：1d｜状态：**todo**
> 上游：PRD FR-11 FR-08；ADR-0013 v2；`contracts/quota-and-isolation.md`

## FR 锚点

| FR | GWT | 本票是否完整覆盖 |
|---|---|---|
| FR-11 | GWT-11.1, 11.2, 11.3 | 是 |
| FR-08 | GWT-08.3 导出/结果越权 | 部分（导出硬顶在 T-10） |

## 边界

**本票做什么**：配额 COUNT、数据中心、租户结果列表、导出过滤 `source <> 'marketplace'`。候选列表平台态 SQL 分页。入队/结果 `tenant_id` NOT NULL（触发者或占位租户）。**不改** harvester 出口。

**本票不做什么**：新建候选表；放宽 017；改 Scrapy。

**会改哪些文件**：

| 文件 | 改动性质 |
|---|---|
| `backend/services/quota_service.py` | 修改：COUNT 谓词 |
| `backend/services/spider_query_service.py` | 修改：列表/导出过滤 |
| `backend/services/skill_service.py` | 修改：`list_candidates` 分页 |
| 对应测试 | 新增 |

**不许改的文件**：`scrapy/spiders/skill_harvester.py`；`power_market/`（包本波可能还不存在）

## 依赖

| 前置票 | 需要它提供什么 |
|---|---|
| T-05 | 入队带租户，避免 NULL 插入 |

## 验收清单

- [ ] GWT-11.1：50 条候选 + A 的 10 条采集 → A 用量存储=10
- [ ] GWT-11.2：无候选时超管审核空态不把租户采集列进来
- [ ] GWT-11.3：A 的数据中心看不到候选正文；导出拖不走候选库

## 自测命令

```bash
uv run pytest -x -q backend/tests/test_saas_quota.py
bash scripts/check-arch.sh
```

## 进度记录

| 时间 | 动作 | 说明 |
|---|---|---|
| | | |

## Evidence（交票前必填）

```
$ <命令>
<输出>
exit: 0
```

## 交票自检

- [ ] 每条验收项有 evidence（命令 + 退出码原样）
- [ ] 自测全绿
- [ ] 只改了「会改哪些文件」里列出的文件
- [ ] 未触碰「不许改的文件」
- [ ] 发现的上游问题已回报，不是自行绕过
- [ ] 状态已更新为 done
