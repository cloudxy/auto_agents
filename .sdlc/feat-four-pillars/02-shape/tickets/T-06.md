# T-06 · 真实 LLM 调用走租户月度配额

> 泳道：L4｜角色：`/backend`｜粗估：1d｜状态：**todo**
> 上游：PRD FR-10；ADR-0014；`contracts/quota-and-isolation.md`

## FR 锚点

| FR | GWT | 本票是否完整覆盖 |
|---|---|---|
| FR-10 | GWT-10.1, 10.2, 10.3 | 是 |

## 边界

**本票做什么**：在 `llm_chat` 调模型前调用 `QuotaService.check_llm_tokens_month`，月份用 Asia/Shanghai。规划/试采修复/技能评分均经此单点。用户可见超限文案走 FR-12 词表（完整用量页在 T-08）。套餐未尽而 provider 预算尽不得映射成 `QUOTA_EXCEEDED` 字样。

**本票不做什么**：用量页 UI（T-08）；接入 new-api 当规划 backend（Q-LLM）；Redis 月度 field expand-contract（记债给 dba）。

**会改哪些文件**：

| 文件 | 改动性质 |
|---|---|
| `backend/services/ai_planner/llm_client.py` | 修改：闸 |
| `backend/tests/test_saas_quota.py` 等 | 修改：经规划路径，不只测 QuotaService |

**不许改的文件**：`newapi.py`；`mcp_bridge.py` 除「若走模型则过闸」外不扩 call_tool

## 依赖

无。

## 验收清单

- [ ] GWT-10.1：额度未尽时规划成功，用量增加，用量页本月 token 前进（与 T-08 联调数字）
- [ ] GWT-10.2：用尽后再规划不走成功路径；文案「本月 LLM Token 已达上限」；用量不再增加
- [ ] GWT-10.3：A 尽、B 有余额：A 仍拒；不扣 B

## 自测命令

```bash
uv run pytest -x -q backend/tests/test_saas_quota.py backend/tests/test_saas_byok.py backend/tests/test_ai_planner.py
bash scripts/check-arch.sh
```

## 进度记录

| 时间 | 动作 | 说明 |
|---|---|---|
| | | |

## Evidence（交票前必填）

```
$ <命令>
<输出>
exit: 0
```

## 交票自检

- [ ] 每条验收项有 evidence（命令 + 退出码原样）
- [ ] 自测全绿
- [ ] 只改了「会改哪些文件」里列出的文件
- [ ] 未触碰「不许改的文件」
- [ ] 发现的上游问题已回报，不是自行绕过
- [ ] 状态已更新为 done
