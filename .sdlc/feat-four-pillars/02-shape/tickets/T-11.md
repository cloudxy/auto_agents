# T-11 · 管理详情去路径、未发布当 404

> 泳道：L4｜角色：`/backend`｜粗估：0.5d｜状态：**todo**
> 上游：PRD FR-13；`contracts/market-public-api.md`（Wave 0 先去字段）

## FR 锚点

| FR | GWT | 本票是否完整覆盖 |
|---|---|---|
| FR-13 | GWT-13.1, 13.2, 13.3 | 是（Wave 1 上架字段落地后本 FR 仍有效） |

## 边界

**本票做什么**：管理 `GET /capabilities/{type}/{name}` 与技能管理详情白名单去掉 `file_path`。公开未发布继续 404 同不存在。租户不能靠公开地址枚举黑名单名称。

**本票不做什么**：`resolve_origin_path`（Wave 1 D15）；上架闸（Wave 1）。

**会改哪些文件**：

| 文件 | 改动性质 |
|---|---|
| `backend/app/api/v1/capabilities.py` | 修改：投影约 193 行去掉 file_path |
| 技能详情 schema/路由 | 修改 |
| `backend/tests/test_skill_public_api.py` | 扩展：管理详情无路径 |

**不许改的文件**：前端商店（Wave 1）；磁盘 SKILL.md

## 依赖

无。

## 验收清单

- [ ] GWT-13.1：已发布公开详情无本机目录路径
- [ ] GWT-13.2：未发布/未上架/黑名单公开地址与页面不存在相同，不出现「已被拉黑」
- [ ] GWT-13.3：租户请求管理详情响应不含本机路径

## 自测命令

```bash
uv run pytest -x -q backend/tests/test_skill_public_api.py backend/tests/test_b1c_capabilities_coverage.py
bash scripts/check-arch.sh
```

## 进度记录

| 时间 | 动作 | 说明 |
|---|---|---|
| | | |

## Evidence（交票前必填）

```
$ <命令>
<输出>
exit: 0
```

## 交票自检

- [ ] 每条验收项有 evidence（命令 + 退出码原样）
- [ ] 自测全绿
- [ ] 只改了「会改哪些文件」里列出的文件
- [ ] 未触碰「不许改的文件」
- [ ] 发现的上游问题已回报，不是自行绕过
- [ ] 状态已更新为 done
