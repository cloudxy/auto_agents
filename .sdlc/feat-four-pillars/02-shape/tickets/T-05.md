# T-05 · 定时/模板/AI 试采入队带租户

> 泳道：L4｜角色：`/backend`｜粗估：1d｜状态：**todo**
> 上游：PRD FR-08 FR-09；ADR-0013（入队 NOT NULL）；`contracts/quota-and-isolation.md`

## FR 锚点

| FR | GWT | 本票是否完整覆盖 |
|---|---|---|
| FR-08 | GWT-08.1, 08.2 | 部分（结果隔离已有 Mixin；本票堵无主任务） |
| FR-09 | GWT-09.1, 09.2, 09.3 | 是 |

## 边界

**本票做什么**：`SpiderService.enqueue` 转发 `tenant_id`。调度 `_fire` 传 `schedule.tenant_id`；模板传模板租户；AI 试采传规划/用户租户。缺租户拒绝入队。并发闸因此生效。修 `test_saas_wiring.py` 末句 `or True` 空心断言。

**本票不做什么**：候选配额过滤（T-07）；Worker 在线检查（FR-71 Wave 4）。

**会改哪些文件**：

| 文件 | 改动性质 |
|---|---|
| `backend/services/spider_service.py` | 修改：enqueue 转发 |
| `backend/services/schedule_service.py` | 修改：`_fire` |
| `backend/services/spider_registry_service.py` | 修改：`create_task_from_template` |
| `backend/services/ai_planner/orchestrator.py` | 修改：`_execute_test` |
| `backend/tests/test_saas_wiring.py` | 修改：去掉 `or True`；补三条路径 |

**不许改的文件**：`scrapy/`；`quota_service.py` 公式（T-06/T-07）

## 依赖

无。

## 验收清单

- [ ] GWT-09.1：模板 / AI 试采 / 到期定时各 1 条 → 都在提交者企业任务列表，并发按 A 计
- [ ] GWT-09.2：并发已满时新任务不开始；A 看到上限；无无主任务
- [ ] GWT-09.3：B 看不到该任务；不计入 B 配额
- [ ] GWT-08.1：A 只见 A 的任务

## 自测命令

```bash
uv run pytest -x -q backend/tests/test_saas_wiring.py backend/tests/test_spider_task_flow.py backend/tests/test_ai_planner.py
bash scripts/check-arch.sh
```

## 进度记录

| 时间 | 动作 | 说明 |
|---|---|---|
| | | |

## Evidence（交票前必填）

```
$ <命令>
<输出>
exit: 0
```

## 交票自检

- [ ] 每条验收项有 evidence（命令 + 退出码原样）
- [ ] 自测全绿
- [ ] 只改了「会改哪些文件」里列出的文件
- [ ] 未触碰「不许改的文件」
- [ ] 发现的上游问题已回报，不是自行绕过
- [ ] 状态已更新为 done
