# T-13 · 产品事件存储与查询面

> 泳道：L4｜角色：`/dba` `/backend`｜粗估：1.5d｜状态：**todo**
> 上游：PRD FR-15；ADR-0016；`contracts/product-events.md`；`metrics-blueprint.md` §5
> **禁止手写 Alembic SQL。** architect 不改事件名。

## FR 锚点

| FR | GWT | 本票是否完整覆盖 |
|---|---|---|
| FR-15 | GWT-15.1 查询面存在 | 部分（埋点发射在 T-14） |
| FR-15 | GWT-15.3 越权 | 查询面守卫 |

## 边界

**本票做什么**：按 §7 语义落产品事件实体（autogenerate）。超管查询 API。写入叶子失败不抛到请求。TTL/保留策略在 db-spec 写清（≥90 天）。

**本票不做什么**：官网/登录/任务埋点调用（T-14）；数仓；改 operation_logs 当漏斗。

**会改哪些文件**：

| 文件 | 改动性质 |
|---|---|
| `platform_core/models/` 新模型 | 新增 |
| `platform_core/schemas/` | 新增 |
| Alembic revision（autogenerate） | 新增 |
| `backend/app/api/v1/admin/` 查询路由 | 新增 |
| `backend/services/` 事件写入叶子 | 新增 |
| 测试 | 新增 |

**不许改的文件**：`operation_logs` 语义；市场事件名（T-35 再用同一表）

## 依赖

无。

## 验收清单

- [ ] 超管按时间 + event_name 能查出插入的行
- [ ] 租户调查询 API 403
- [ ] 写入函数失败时调用方仍成功（单测 mock 写失败）
- [ ] `MYSQL_FIDELITY=1 uv run alembic revision --autogenerate` 对人审；`bash scripts/check-db-migrations.sh`

## 自测命令

```bash
uv run pytest -x -q backend/tests -k product_event
bash scripts/check-db-migrations.sh
bash scripts/check-arch.sh
```

## 进度记录

| 时间 | 动作 | 说明 |
|---|---|---|
| | | |

## Evidence（交票前必填）

```
$ <命令>
<输出>
exit: 0
```

## 交票自检

- [ ] 每条验收项有 evidence（命令 + 退出码原样）
- [ ] 自测全绿
- [ ] 只改了「会改哪些文件」里列出的文件
- [ ] 未触碰「不许改的文件」
- [ ] 发现的上游问题已回报，不是自行绕过
- [ ] 状态已更新为 done
