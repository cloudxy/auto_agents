# 定义帽审查账本 · feat-four-pillars

> 日期：2026-09-07  
> 现行 PRD：`01-define/spec.md` **v1.3**  
> 独立审查最后一次：第 3 轮（PM v1.2）**不通过**（QA-29 major / QA-30 minor）。  
> PM v1.3 自称关闭 QA-29/QA-30，**尚未**第 4 轮 G-fresh。producer 自批不算过闸。  
> 第 2 轮正文保留为历史；文内 QA-20…QA-28 的「open」已被第 3 轮独立核对为 fixed。

审查范围：`01-define/spec.md`、`user-story.md`、`metrics-blueprint.md`、`state.yaml`、`diagnosis/pm.md`、既有 findings、Power Market 已过审设计、`CONTEXT.md`、宪法。审查人不改文件。不采信 producer 自批。

---

## 上一轮处置（独立核对）

| ID | 原严重度 | 本轮状态 |
|---|---|---|
| QA-01 | blocker | **fixed** |
| QA-02 | blocker | **fixed**（GWT-15.1/15.4 含候选标记 / spider / source / login_failed / 企业关联） |
| QA-03 … QA-19 | major/minor | **fixed**（残留另立新号，不回写旧条） |

---

## FINDINGS

### QA-20 NFR-05 把租户安装写成「本企业登录者」，与已冻结的只读拒绝互相否定

- **维度**：4 安全 / 6 契约一致性
- **严重度**：major
- **证据**：`spec.md` NFR-05「租户安装 = 本企业登录者」；同文件权限矩阵与 GWT-20.5 冻结只读不能订
- **修复建议**：NFR-05 改成与矩阵同一句话（经办或负责人；只读拒绝）
- **状态**：open

### QA-21 只读改本企业安装写在矩阵「不能做」，FR-21 越权格只测跨租户

- **维度**：4 安全 / 8 边界
- **严重度**：major
- **证据**：矩阵只读不能改启用/信任/卸载；GWT-21.3 只覆盖跨租户
- **修复建议**：FR-21 增越权 GWT：只读点启用/信任/卸载 → 拒绝、安装行不变、文案「请联系企业管理员」
- **状态**：open

### QA-22 spec §6 宣称与蓝图逐条一致，三行口径仍比蓝图窄

- **维度**：6 契约一致性 / 3 证据有效性
- **严重度**：major
- **证据**：spec 护栏未写安装/成员/出站钥匙混拉、未放行许可含 Moonshot、排除候选、「近 7 日结果」同窗
- **修复建议**：spec §6 护栏表改成蓝图第 4 节复制，或删掉「逐条一致」只留「以蓝图为准」
- **状态**：open

### QA-23 Wave 1 未冻 host_compat 为空或 NULL 时能不能订

- **维度**：1 标准符合 / 8 边界
- **严重度**：major
- **证据**：GWT-20.4 只覆盖「名单不含 Grok」；设计 `host_compat JSON NULL`；FR-31 第一方已发布必须已上架
- **修复建议**：冻结 NULL/未声明 = 四个宿主都可订（或 = 全部不可订），并点名第一方 FR-31 走哪条
- **状态**：open

### QA-24 FR-29 把安装行「启用」写成「启用到宿主」，GWT 只钉信任开关

- **维度**：2 标准质量 / 6 契约一致性
- **严重度**：major
- **证据**：FR-29 用「启用到宿主」；GWT-26.1 Wave 1 无该按钮；GWT-29.3 只断言开信任拒绝
- **修复建议**：改用安装行词汇（启用/信任/卸载）；GWT-29 补改启用拒绝
- **状态**：open

### QA-25 PRD 仍断言 `state.yaml` 写着 8h

- **维度**：3 证据有效性 / 6 契约一致性
- **严重度**：minor
- **修复建议**：删掉「现写 8h」；改成进度源已按波
- **状态**：open

### QA-26 TTFV 用首次 `task_completed`，不排除市场候选

- **维度**：6 契约一致性
- **严重度**：minor
- **修复建议**：TTFV 改为首次 `is_marketplace_candidate=false` 且 `result_count>0`
- **状态**：open

### QA-27 同一能力订到第二个宿主：设计唯一键允许多行，故事写「之一」，无 GWT

- **维度**：6 契约一致性 / 8 边界
- **严重度**：minor
- **修复建议**：补边界 GWT：已订 Grok 再订 Claude → 两行都在 / 或拒绝并保持一行
- **状态**：open

### QA-28 渠道页对租户：GWT-07.3 进页拒绝，词表要求平台专属隐藏

- **维度**：6 契约一致性
- **严重度**：minor
- **修复建议**：Wave 0 冻一种：隐藏（直打 URL 与 404 相同）或非道歉拒绝。GWT 与 §3.2 只留一个 oracle
- **状态**：open

---

## 总计

| 严重度 | 数量 | 处置 |
|---|---|---|
| blocker | 0 | 上一轮 2 条均 fixed |
| major | 5 | QA-20…QA-24 **open** |
| minor | 4 | QA-25…QA-28 **open** |

**推荐处置：** 关闭 QA-20/21/23/24（建议连带 QA-22）之前，不要把 Wave 0+1 当可实施冻结集交给 `/architect`。

**合格点：** Working Backwards 四句；真/伪分诊含 T-31/T-32；RICE 未装市场规模；GWT-15 已能支撑 WACT 排除候选；只读订阅、宿主不匹配灰掉+拒绝、XSS、测试中矩阵、Q-AGPL、首页失败装空、出站 Key 无绑定拒绝、别名冲突、§9.2 分发≠验证均已写进冻结集。操作者七问未代选。

---

## 第 3 轮（PM v1.2）· session `01a07b08-f255-7093-9245-4fce49ee12df`

结论：**不通过**。QA-20…QA-28 九条独立核对为 **fixed**。新 major **QA-29** 仍开。

### QA-29 GWT-14.3 仍把租户「查看渠道页」写成能看见掩码密钥，与已冻的隐藏+404 互相否定

- **维度**：4 安全 / 6 契约一致性
- **严重度**：major
- **证据**：GWT-14.3 Given 租户成员查看系统设置或渠道页 Then 密钥掩码；GWT-07.3 / FR-07 / §3.2 渠道页对租户隐藏、直打同 404
- **修复建议**：GWT-14.3 拆开：渠道页走 GWT-07.3；若租户仍能打开的系统设置上有密钥字段，则掩码
- **状态**：open（第 3 轮）。**producer 声称 v1.3 已关**（GWT-14.3/14.4 拆分）——待第 4 轮独立核对。

### QA-30 柱 A「凭什么说改善了」未排除市场候选

- **维度**：6 契约一致性
- **严重度**：minor
- **修复建议**：§1.1 与 WACT/TTFV 同一句话（排除候选）
- **状态**：open（第 3 轮）。**producer 声称 v1.3 已关**（§1.1 与 WACT 同口径）——待第 4 轮独立核对。

| 严重度 | 第 3 轮独立审查 open | 第 4 轮 |
|---|---|---|
| blocker | 0 | 未跑 |
| major | 1（QA-29） | 待核对 v1.3 |
| minor | 1（QA-30） | 待核对 v1.3 |
