# T-20 · 只读成员页隐藏写控件

> 泳道：L4｜**lane：ui**｜并行组：**P0**｜角色：`/frontend` admin｜粗估：0.5–1d｜状态：**todo**
> 上游：spec FR-89 · NFR-05 · edge-states 屏「成员管理」
> 点了再失败不算藏。

## FR 锚点

| FR | GWT | 本票是否完整覆盖 |
|---|---|---|
| FR-89 | GWT-89.1, 89.2, 89.3 | 是 |

## 边界

**本票做什么**：只读看不见添加成员、角色下拉、重置密码、删除；名单仍可见。强提交仍拒。经办本波不开放成员写。

**本票不做什么**：采集写按钮已藏保持；不改成员 API 角色模型。

**会改哪些文件**：

| 文件 | 改动性质 |
|---|---|
| `frontend/admin/src/pages/Members.tsx` | 修改：只读/经办不渲染写控件 |
| `frontend/admin/src/pages/Members.test.tsx` | 修改 |

**不许改的文件**：`backend/services/member_service.py` 除强提交拒绝已存在则只补测；`Settings.tsx`（T-21）。

## 依赖

| 前置票 | 需要它提供什么 | 状态 |
|---|---|---|
| — | 无 | — |

**并行组 P0。** 与 T-17/T-21 各页，文件不重叠。

## expand-contract

无 DDL。前端藏控件是体验；后端必须独立拒（GWT-89.3）。

## GWT / Then（从 spec 抄）

- **GWT-89.1**：Then 看不见「添加成员」、角色下拉、重置密码、删除；能看见成员名单。
- **GWT-89.2**：Then 能看见自己；只读打开同一企业仍满足 89.1。
- **GWT-89.3**：Then 拒绝；成员名单不变。

可见处无 `FORBIDDEN` 原文。

## 自测命令

```bash
npm test --prefix frontend/admin -- --testPathPattern=Members.test
uv run pytest -x -q backend/tests/test_saas_members.py
```

## 进度记录

| 时间 | 动作 | 说明 |
|---|---|---|
| | | |

## Evidence（交票前必填）

```
$ <命令>
<输出>
exit: 0
```

## 交票自检

- [ ] 只读 DOM 无写控件（不是点了再 403）
- [ ] 名单仍可见；强提交名单不变
