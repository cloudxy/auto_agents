# T-13 · 公开商店/精选过滤测试种子

> 泳道：L4｜**lane：api**｜并行组：**P0**｜角色：`/backend`｜粗估：1d｜状态：**todo**
> 上游：spec FR-80 · contract §7.5 · db-spec §13 **种子不进 schema** · PIT-5 双公开闸

## FR 锚点

| FR | GWT | 本票是否完整覆盖 |
|---|---|---|
| FR-80 | GWT-80.1, 80.2, 80.3, 80.4 | 是 |

## 边界

**本票做什么**：公开列表/精选/total **都不含**测试种子。谓词与 GWT-80.1 同一套：短名 `nfr01qc2-*`（大小写不敏感）∨ 标题以「NFR卡片」开头 ∨ 描述含 `preprod nfr-01 seed`。`/public/skills` 与 `/public/capabilities` **同一**谓词，两份测试同 PR。租户不能把种子改成访客可见。夹具 `example-pdf-extractor` 清污染后可搜（对照，不新开模型）。

**本票不做什么**：页大小≤20 / 第 21 张 / 翻页事件 → T-14；FR-91；加 `is_seed` 列。

**会改哪些文件**：

| 文件 | 改动性质 |
|---|---|
| `backend/services/power_market/service.py` | 修改：`list_public` 残差滤种子（FR-33 可见闸之后） |
| `backend/app/api/v1/public_skills.py` | 修改：同一谓词 |
| `backend/app/api/v1/capabilities.py`（公开列表路径） | 修改：同一谓词 |
| `backend/tests/test_skill_public_api.py` | 修改：PIT-5 |
| `backend/tests/test_b1c_capabilities_coverage.py` | 修改：PIT-5 同 PR |
| `backend/tests/test_t28_listing.py`（若钉 listed 种子） | 对照 |

**不许改的文件**：`capability_assets` DDL（禁止 `is_seed`）；`frontend/official` 翻页控件（T-14）；`relay_*`。

## 依赖

| 前置票 | 需要它提供什么 | 状态 |
|---|---|---|
| — | 无 | — |

**并行组 P0。**

## expand-contract

**种子不进 schema。** 无 `is_seed` / `is_fixture`。查询谓词 + 治理/同步不得把上述行标 listed 当商品。

## GWT / Then（从 spec 抄）

- **GWT-80.1**：Then 该卡不出现；列表总件不含它。
- **GWT-80.2**：Then 「还没有上架的能力」；不是失败句；失败仍走已兑「市场列表加载失败 / 重试」。
- **GWT-80.3**：Then 公开商店仍不出现该卡（治理台是否可见不在本 GWT 规定）。
- **GWT-80.4**：Then 结果含该行（本 GWT 不把「商店里没有夹具」解释成市场模型未做）。

## 自测命令

```bash
uv run pytest -x -q backend/tests/test_skill_public_api.py backend/tests/test_b1c_capabilities_coverage.py
```

## 进度记录

| 时间 | 动作 | 说明 |
|---|---|---|
| | | |

## Evidence（交票前必填）

```
$ <命令>
<输出>
exit: 0
```

## 交票自检

- [ ] 双公开端 + 两份测试同 PR
- [ ] 无 `is_seed` 列
- [ ] `example-pdf-extractor` 对照仍可搜
