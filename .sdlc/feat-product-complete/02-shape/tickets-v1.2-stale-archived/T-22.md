# T-22 · 产品事实：fail-open + 租户无查询面

> 泳道：L4｜**lane：api**｜并行组：**P2**｜角色：`/backend`｜粗估：1d｜状态：**todo**
> 上游：spec FR-92 GWT-92.6/92.7 · NFR-08 · contract §7.7
> **PC-3 完成线与 T-02 同（看见已确认，不是任意一条）。** 生产事件名由 T-02/05/09/14 写入。

## FR 锚点

| FR | GWT | 本票是否完整覆盖 |
|---|---|---|
| FR-92 | GWT-92.6, 92.7 | 是 |
| FR-92 | GWT-92.1…92.5 | 否（生产侧已在前置票） |

## 边界

**本票做什么**：上报失败主路径仍成功（申请已挂上、钥匙已签发、列表已翻页）。租户打开查询面 404 同形。不改事件名/WACT。查询面仅超管。事件不含明文钥匙/密码/master。

**本票不做什么**：不改北极星 WACT；不建仓；不把 PC-3 改成「任意一条事件」。五件事件生产在前置票。

**会改哪些文件**：

| 文件 | 改动性质 |
|---|---|
| `backend/services/product_event_service.py` | 修改：emit fail-open |
| `backend/app/api/v1/product_events.py` | 对照：租户 404（`require_platform_admin_or_404`） |
| `backend/tests/test_product_events.py` | 修改：92.6/92.7 |
| `backend/tests/test_t32_market_events.py` | 对照 |

**不许改的文件**：事件名字面量；WACT 口径文件；`billing_service.py` 写规则（T-01/T-02）。

## 依赖

| 前置票 | 需要它提供什么 | 状态 |
|---|---|---|
| T-02 | `offline_order_submitted` / `offline_order_confirmed` | todo |
| T-05 | `outbound_key_issued` | todo |
| T-09 | `relay_token_call_succeeded` | todo |
| T-14 | `market_list_paged` | todo |

**并行组 P2。** 查询/fail-open 可在前置票事件名已进合法集后合入。

## expand-contract

`product_events` **不加列**。只追加合法 `event_name` 五件（前置票已加则本票只收口 fail-open）。访客 `market_list_paged`：`tenant_id` NULL + `anonymous_id`。

## GWT / Then（从 spec 抄）

- **GWT-92.6**：Then 主路径仍成功（申请已挂上、钥匙已签发、列表已翻页）。
- **GWT-92.7**：Then 与「页面不存在」同形（v2 仍真）。

QA-24：PC-3 完成线与 T-02 对齐 = 租户看见 **已确认**，不是查询面任意一条事实。

## 自测命令

```bash
uv run pytest -x -q backend/tests/test_product_events.py backend/tests/test_t32_market_events.py
```

## 进度记录

| 时间 | 动作 | 说明 |
|---|---|---|
| | | |

## Evidence（交票前必填）

```
$ <命令>
<输出>
exit: 0
```

## 交票自检

- [ ] 上报失败不挡下单/签发/翻页
- [ ] 租户查询面 404 同形
- [ ] 未改事件名/WACT；PC-3 仍=已确认
