# T-16 · 注册邮箱能登录

> 泳道：L4｜**lane：api**｜并行组：**P0**｜角色：`/backend` + official｜粗估：1d｜状态：**todo**
> 上游：spec FR-83 · contract §7.6 · users.email 已全局 UNIQUE（**不动 DDL**）
> PC-1 不把短名成功算邮箱已修。

## FR 锚点

| FR | GWT | 本票是否完整覆盖 |
|---|---|---|
| FR-83 | GWT-83.1, 83.2, 83.3, 83.4, 83.5 | 是 |

## 边界

**本票做什么**：主字段填 **注册邮箱** + 正确密码 → 进该企业。标识含 `@` 走 email 查找（全局唯一），否则仍 username 消歧。未知标识与错密码 **同句**；到期 **不同句**。注册成功屏写明「登录时请填写注册邮箱」。

**本票不做什么**：不把登录页改成纯「邮箱」审美（label 可不改）；不加 `users.email` 列；不代选 Q-VOICE。

**会改哪些文件**：

| 文件 | 改动性质 |
|---|---|
| `backend/services/auth_service.py` | 修改：`authenticate` 含 `@` → email |
| `backend/app/api/v1/auth.py` | 对照 |
| `backend/tests/test_auth_login_tenant.py` | 修改 |
| `backend/tests/test_auth_service.py` | 修改 |
| `frontend/admin/src/pages/Login.tsx` | 修改：placeholder「注册邮箱或用户名」 |
| `frontend/admin/src/pages/Login.test.tsx` | 修改 |
| `frontend/official/src/pages/Register.tsx` | 修改：成功屏句 |
| `frontend/official/src/pages/Register.test.tsx` | 修改 |

**不许改的文件**：到期句已兑 FR-08（只保证与 83.2 不同句）；`users` DDL。

## 依赖

| 前置票 | 需要它提供什么 | 状态 |
|---|---|---|
| — | 无 | — |

**并行组 P0。**

## expand-contract

无 DDL。查找路径变更，不是加列。

## GWT / Then（从 spec 抄）

- **GWT-83.1**：Then 进入该企业后台。
- **GWT-83.2**：Then 「用户名或密码不正确。核对后再登录。」；不进入后台。
- **GWT-83.3**：Then 写明「登录时请填写注册邮箱」；若仍展示短登录名，不得暗示「只能用短登录名」而导致 83.1 失败。
- **GWT-83.4**：Then 进入企业 A，不进入企业 B。
- **GWT-83.5**：Then 失败句与 GWT-83.2 **不同句**（已兑 FR-08 仍真）。

code：`AUTH_BAD_CREDENTIALS` 同句；`AUTH_TENANT_EXPIRED` 不同句。邮箱/短名填错 **同句**（GWT-83.2）。

## 自测命令

```bash
uv run pytest -x -q backend/tests/test_auth_login_tenant.py backend/tests/test_auth_service.py
npm test --prefix frontend/admin -- --testPathPattern=Login.test
npm test --prefix frontend/official -- --testPathPattern=Register.test
```

## 进度记录

| 时间 | 动作 | 说明 |
|---|---|---|
| | | |

## Evidence（交票前必填）

```
$ <命令>
<输出>
exit: 0
```

## 交票自检

- [ ] 邮箱+正确密码进该企业；短名成功 ≠ 本票完成
- [ ] 未知标识=错密码同句；到期不同句
- [ ] 成功屏有「登录时请填写注册邮箱」
