# T-23 · 失败≠空：超管待确认收款 + 产品事实 Tab

> 泳道：L4｜**lane：ui**｜并行组：**P0**｜角色：`/frontend` admin｜粗估：1d｜状态：**todo**
> 上游：spec FR-84 GWT-84.3/84.4 · edge-states 屏「超管待确认收款」「平台运营台 / 产品事实」

## FR 锚点

| FR | GWT | 本票是否完整覆盖 |
|---|---|---|
| FR-84 | GWT-84.3, 84.4 | 是 |
| FR-84 | GWT-84.1, 84.2 | 否 → T-17 |

## 边界

**本票做什么**：租户打开运营台待确认收款或产品事实 = 后台 404 同形，**不是空表**。超管列表失败 ≠ 默认「暂无数据」。真 0 pending：「还没有待确认的收款。」

**本票不做什么**：确认收款业务规则 → T-02；fail-open API → T-22；点名五屏 → T-17。确认成功 **不**把配额履约写成 Then。

**会改哪些文件**：

| 文件 | 改动性质 |
|---|---|
| `frontend/admin/src/pages/PlatformOps.tsx` | 修改：待确认 Tab 失败句+重试；空态句 |
| `frontend/admin/src/pages/ProductEvents.tsx` | 修改：失败≠空 |
| `frontend/admin/src/pages/ProductEvents.test.tsx` | 修改 |
| `frontend/admin/src/App.tsx` / `ProtectedRoute.tsx` | 对照：租户直打 `/platform-ops` 404 同形 |

**不许改的文件**：`Dashboard.tsx` 等 T-17 五屏；`billing_service.py`。

## 依赖

| 前置票 | 需要它提供什么 | 状态 |
|---|---|---|
| — | 无（失败壳不依赖确认 API 扩） | — |

**并行组 P0。** 与 T-15 同形 404：本票点名 `/platform-ops` 已在 `PLATFORM_WRITE_KEYS`；T-15 组织页。合入勿把设置页卷进 404（T-21）。

## expand-contract

无 DDL。叶位置：运营管理组 `/platform-ops` Tab，不新组。

## GWT / Then（从 spec 抄）

- **GWT-84.3**：Then 与「页面不存在」同形，不是空表。
- **GWT-84.4**：Then 失败句 + 不是默认「暂无数据」。

edge-states：空态「还没有待确认的收款。」产品事实空态「还没有产品事实。」失败「…加载失败。检查网络后重试。」

## 自测命令

```bash
npm test --prefix frontend/admin -- --testPathPattern='ProductEvents.test|App.test'
npm run build --prefix frontend/admin
```

## 进度记录

| 时间 | 动作 | 说明 |
|---|---|---|
| | | |

## Evidence（交票前必填）

```
$ <命令>
<输出>
exit: 0
```

## 交票自检

- [ ] 租户直打运营台 404 同形不是空表
- [ ] 超管失败 ≠「暂无数据」
