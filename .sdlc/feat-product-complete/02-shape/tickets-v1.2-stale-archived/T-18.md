# T-18 · 无工人：去节点 + 新建提交被拦住

> 泳道：L4｜**lane：ui**｜并行组：**P0**｜角色：`/frontend` admin｜粗估：1d｜状态：**todo**
> 上游：spec FR-85 · X-WORKER · edge-states 屏「采集任务」「新建任务弹窗」「节点监控」
> **没有**「提交后立即可见」支。不重开出数环。

## FR 锚点

| FR | GWT | 本票是否完整覆盖 |
|---|---|---|
| FR-85 | GWT-85.1, 85.2, 85.3 | 是 |

## 边界

**本票做什么**：打开任务页立即「采集未运行，不会出数」**并且**「去节点」打开 `/spiders/nodes`。新建提交 **拦住**、任务未入队、**不得** toast「正在排队执行」。只读无提交。

**本票不做什么**：不重开 FR-18/19 出数环；不改 Scrapy；满额信封 → T-12（两 Then 并存）。

**会改哪些文件**：

| 文件 | 改动性质 |
|---|---|
| `frontend/admin/src/pages/Spiders.tsx` | 修改：横幅 +「去节点」 |
| `frontend/admin/src/pages/Spiders.test.tsx` | 修改 |
| `frontend/admin/src/components/spider/TaskModal.tsx` | 修改：0 工人拦住；删「正在排队执行」成功 toast 在该支 |
| `frontend/admin/src/pages/Nodes.tsx` | 对照：落地页；空态禁止心跳实现细节句 |
| `backend/services/spider_worker_gate.py` | 对照：入队闸仍在；本票 UI 必须在打开页就可见，不只靠提交失败 |

**不许改的文件**：Scrapy 管道；Redis 协议。

## 依赖

| 前置票 | 需要它提供什么 | 状态 |
|---|---|---|
| — | 无 | — |

**并行组 P0。** 与 T-12 同改 TaskModal：工人闸 vs 配额闸分分支，禁止互删。

## expand-contract

无 DDL。

## GWT / Then（从 spec 抄）

- **GWT-85.1**：Then 看见「采集未运行，不会出数」**并且**有「去节点」；「去节点」打开节点页。
- **GWT-85.2**：Then 被拦住（任务未进入排队）；不得出现「正在排队执行」；不得提交成功。本 Then **没有**「提交后立即看见『现在不会出数』」这一支。
- **GWT-85.3**：Then 无提交入口（已兑隐藏写按钮仍真）。

## 自测命令

```bash
npm test --prefix frontend/admin -- --testPathPattern=Spiders.test
uv run pytest -x -q backend/tests/test_spider_worker_offline.py
```

## 进度记录

| 时间 | 动作 | 说明 |
|---|---|---|
| | | |

## Evidence（交票前必填）

```
$ <命令>
<输出>
exit: 0
```

## 交票自检

- [ ] 打开页立即有「去节点」
- [ ] 提交拦住且无「正在排队执行」
- [ ] 无「提交后立即可见」支
