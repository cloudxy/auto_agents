# T-11 · 值班页看得见伪装；空态不第三套

> 泳道：L4｜**lane：ui**｜并行组：**P0**｜角色：`/frontend` admin + 夹具 backend｜粗估：0.5–1d｜状态：**todo**
> 上游：spec FR-61 · edge-states 屏「值班 `/newapi`」· 词「伪装」已在 VERDICT_TAG

## FR 锚点

| FR | GWT | 本票是否完整覆盖 |
|---|---|---|
| FR-61 | GWT-61.1, 61.2, 61.3 | 是 |

## 边界

**本票做什么**：钉 **探针刚判伪装后页上可见**（计数或行上「伪装」），渠道不自动关。空态只允许已冻 71.2/71.3，禁止第三套「还没有中转站」。租户无入口、直打 404 同形。入口显示名「中转站管控」或「网关」，同一入口。

**本票不做什么**：不换数据面；不重开 GWT-07.6 熔断合同；不焊根编排；不发明电话。

**会改哪些文件**：

| 文件 | 改动性质 |
|---|---|
| `frontend/admin/src/pages/NewApiOps.tsx` | 修改：伪装可见；空态两句 |
| `frontend/admin/src/pages/NewApiOps.test.tsx` | 修改 |
| `frontend/admin/src/components/newapi/ProbeResults.tsx` | 修改：行上 VERDICT_TAG「伪装」 |
| `backend/tests/test_llm_probe.py` | 对照：GWT-07.6 仍真；可加「页夹具刚写入 spoofed」 |

**不许改的文件**：`relay_service.py`；`llm_gateway/chat.py` 路由；租户渠道组页。

## 依赖

| 前置票 | 需要它提供什么 | 状态 |
|---|---|---|
| — | 无 | — |

**并行组 P0。**

## expand-contract

无 DDL。`channel_probe_results.verdict` 已有。

## GWT / Then（从 spec 抄）

- **GWT-61.1**：Then 能看见该次伪装（计数或行上有「伪装」字样）；该渠道保持可用、不自动关闭；窗口与额度不变。
- **GWT-61.2**：Then 只出现已冻的网关空态句（还没配 / 网关挂了）；不出现第三套空态句。
- **GWT-61.3**：Then 无入口；直打与「页面不存在」同形；阈值不变。

edge-states 空态只允许：71.2「还没有平台模型…」；71.3「LLM 网关管理面不可达，仅本地事件/探针」。禁止「暂无渠道」「还没有中转站」。

## 自测命令

```bash
npm test --prefix frontend/admin -- --testPathPattern=NewApiOps.test
uv run pytest -x -q backend/tests/test_llm_probe.py
```

## 进度记录

| 时间 | 动作 | 说明 |
|---|---|---|
| | | |

## Evidence（交票前必填）

```
$ <命令>
<输出>
exit: 0
```

## 交票自检

- [ ] 刚判伪装后页上可见「伪装」且渠道未关
- [ ] 空态无第三套
- [ ] 租户直打 404 同形
