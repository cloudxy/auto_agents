# T-17 · 失败≠空：节点/仪表盘/数据/渠道组/LLM

> 泳道：L4｜**lane：ui**｜并行组：**P0**｜角色：`/frontend` admin｜粗估：2d｜状态：**todo**
> 上游：spec FR-84 GWT-84.1/84.2 · NFR-03 · edge-states §0.2

## FR 锚点

| FR | GWT | 本票是否完整覆盖 |
|---|---|---|
| FR-84 | GWT-84.1, 84.2 | 是（点名五屏） |
| FR-84 | GWT-84.3, 84.4 | 否 → T-23 |

## 边界

**本票做什么**：节点 / 仪表盘 / 数据中心 / 渠道组 / LLM 供应商：接口失败 → 失败句 + 可点「重试」。禁止「暂无数据 / 暂无在线节点 / 还没有采集结果 / 暂无 LLM 供应商，点击新建」；仪表盘卡片 0 不得冒充没跑过。真 0 走「还没有…」。渠道组无令牌走 GWT-60.4（不在本票发明第三套空态）。

**本票不做什么**：超管待确认 + 产品事实 Tab → T-23；渠道组 Base URL/用法 → T-10（本票只钉失败≠空，空态文案不覆盖 T-10）；无工人 → T-18。

**会改哪些文件**：

| 文件 | 改动性质 |
|---|---|
| `frontend/admin/src/pages/Dashboard.tsx` | 修改 |
| `frontend/admin/src/pages/Dashboard.test.tsx` | 修改 |
| `frontend/admin/src/pages/Nodes.tsx` | 修改 |
| `frontend/admin/src/pages/Data.tsx` | 修改 |
| `frontend/admin/src/pages/Data.test.tsx` | 修改 |
| `frontend/admin/src/pages/RelayGroups.tsx` | 修改：失败句（空态 60.4 留给 T-10；本票失败分支先落地） |
| `frontend/admin/src/pages/LlmProviders.tsx` | 修改 |
| `frontend/admin/src/pages/LlmProviders.test.tsx` | 修改 |

**不许改的文件**：`backend/**`（本票壳）；`PlatformOps.tsx`（T-23）。

## 依赖

| 前置票 | 需要它提供什么 | 状态 |
|---|---|---|
| — | 无 | — |

**并行组 P0。** 与 T-10 都动 `RelayGroups` → T-10 在 P1，错开。本票先合失败分支。

## expand-contract

无 DDL。

## GWT / Then（从 spec 抄）

- **GWT-84.1**：Then 出现失败句 + 可点重试；表格或数字 **不是**「暂无数据 / 暂无在线节点 / 还没有采集结果 / 暂无 LLM 供应商，点击新建」；仪表盘卡片 **不是** 用 0 冒充没跑过。
- **GWT-84.2**：Then 各页空态句说明「还没有…」且不是失败句；渠道组无令牌走 GWT-60.4；数据中心「还没有采集结果。完成一次采集后会显示在这里。」

## 自测命令

```bash
npm test --prefix frontend/admin -- --testPathPattern='Dashboard.test|Data.test|LlmProviders.test|RelayGroups.test'
```

## 进度记录

| 时间 | 动作 | 说明 |
|---|---|---|
| | | |

## Evidence（交票前必填）

```
$ <命令>
<输出>
exit: 0
```

## 交票自检

- [ ] 五屏失败≠空；真 0 走「还没有…」
- [ ] 未把渠道组失败画成 60.4 空态
