# T-14 · 公开列表页大小≤20 + 夹具第 21 张 + 翻页事件

> 泳道：L4｜**lane：ui**｜并行组：**P1**｜角色：official + `/backend`｜粗估：1–2d｜状态：**todo**
> 上游：spec FR-81 / FR-92 · NFR-01/02 · contract §7.5 · edge-states 屏「官网能力市场」

## FR 锚点

| FR | GWT | 本票是否完整覆盖 |
|---|---|---|
| FR-81 | GWT-81.1, 81.2, 81.3, 81.4 | 是 |
| FR-92 | GWT-92.5 | 是 |
| NFR-01 | P95&lt;2s | 计时交 verify，本票不造假秒数 |
| NFR-02 | 第一页最多 20 | 是 |

## 边界

**本票做什么**：公开端 `PAGE_SIZE_MAX` 收到 **20**。仅当非种子 listed>20 有下一页。≥21 **用夹具资产**，禁止再造商店模型、禁止把种子改成商品。`market_list_paged` 含 `page`、`result_count`、`anonymous_id`；访客无 `tenant_id`。

**本票不做什么**：种子谓词 → T-13；NFR-01 浏览器计时 → verify 阶段；FR-91。

**会改哪些文件**：

| 文件 | 改动性质 |
|---|---|
| `backend/services/power_market/types.py` | 修改：公开 `PAGE_SIZE_MAX=20`（租户/超管端若仍 50 须分闸，禁止公开 50） |
| `backend/app/api/v1/public_skills.py` | 修改：`le=PAGE_SIZE_MAX` |
| `frontend/official/src/pages/Capabilities.tsx` | 修改：翻页控件；listed≤20 无假下一页 |
| `frontend/official/src/pages/Capabilities.test.tsx` | 修改 |
| `frontend/official/src/pages/Home.tsx` | 对照：精选 0 张种子（T-13 谓词） |
| `backend/services/product_event_service.py` / `market_events.py` | 修改：`market_list_paged` |
| 夹具（测试数据，非商品模型） | 21 张非种子 listed |

**不许改的文件**：为翻页新建商店模型；把 `nfr01qc2-*` 改成商品。

## 依赖

| 前置票 | 需要它提供什么 | 状态 |
|---|---|---|
| T-13 | 种子已从 total/列表去掉，否则翻页被种子撑出 | todo |

**并行组 P1。**

## expand-contract

查询闸收紧：公开 `page_size` 最大 20。无 DDL。种子仍不进 schema。

## GWT / Then（从 spec 抄）

- **GWT-81.1**：Then 第一页 ≤20 张；页上能看见总件数与下一页；点下一页后第 21 张可到达且不与第一页整页重复。
- **GWT-81.2**：Then 无「还有下一页」的假控件把人带到空白货架冒充还有商品。
- **GWT-81.3**：Then 空态句；不出现总件>0 或翻页数字谎称有货。
- **GWT-81.4**：Then 任何一页都不出现该资产。
- **GWT-92.5**：Then 上报 `market_list_paged`，含 `page`、`result_count`、`anonymous_id`（访客无 `tenant_id`）。

## 自测命令

```bash
uv run pytest -x -q backend/tests/test_skill_public_api.py backend/tests/test_b1c_capabilities_coverage.py
npm test --prefix frontend/official -- --testPathPattern='Capabilities.test|Home.test'
```

## 进度记录

| 时间 | 动作 | 说明 |
|---|---|---|
| | | |

## Evidence（交票前必填）

```
$ <命令>
<输出>
exit: 0
```

## 交票自检

- [ ] 公开 MAX=20；第 21 张来自夹具不是再造商品
- [ ] 事件含 `anonymous_id`、无访客 `tenant_id`
- [ ] NFR-01 计时留给 verify
