# T-06 · 出站拉数钥匙页

> 泳道：L4｜**lane：ui**｜并行组：**P1**｜角色：`/frontend` admin｜粗估：1d｜状态：**todo**
> 上游：spec FR-51 · edge-states 屏「出站拉数钥匙 `/outbound-keys`」· sitemap 数据工厂组内加叶，紧挨数据中心

## FR 锚点

| FR | GWT | 本票是否完整覆盖 |
|---|---|---|
| FR-51 | GWT-51.2 空态句；51.5/51.8/51.9 控件与明文一次 | 是（UI） |
| FR-51 | GWT-51.1 产品名半格 | 是（h1） |

## 边界

**本票做什么**：数据工厂加叶 `/outbound-keys`。h1 / 产品名 **「出站拉数钥匙」**。空态句；签发/吊销控件按角色；再进页不见明文；入口不得画成渠道组。明文一次 + 复制 toast「已复制出站拉数钥匙」。

**本票不做什么**：API 签发 → T-04；拉数执法 → T-05；渠道组页 → T-10；吊销二次确认（范围外）。

**会改哪些文件**：

| 文件 | 改动性质 |
|---|---|
| `frontend/admin/src/pages/OutboundKeys.tsx` | **新增** |
| `frontend/admin/src/pages/OutboundKeys.test.tsx` | **新增** |
| `frontend/admin/src/services/outboundKeys.ts` | **新增** |
| `frontend/admin/src/App.tsx` | 修改：路由 |
| `frontend/admin/src/config/menuConfig.tsx` | 修改：数据工厂 children **末尾**加叶（不重排组） |

**不许改的文件**：`RelayGroups.tsx`；`AdminLayout.tsx`（T-15）；`backend/**`。

## 依赖

| 前置票 | 需要它提供什么 | 状态 |
|---|---|---|
| T-04 | 签发/列/吊销 API | todo |

**并行组 P1。** 与 T-15：只在 `menuConfig` 工厂组末尾加叶。与 T-03：T-03 加 `/orders` 在概览组，不碰工厂组。

## expand-contract

无 DDL。叶位置锁：`/outbound-keys` 在数据工厂，紧挨 `/data`（edge-states / sitemap）。

## GWT / Then（从 spec 抄）

- **GWT-51.1（产品名）**：Then 页上名称是「出站拉数钥匙」不是「渠道组令牌」。
- **GWT-51.2**：Then 「还没有出站拉数钥匙。签发后才能从外部系统拉本企业结果。」；有权角色看见签发；不是渠道组页冒充本入口。
- **GWT-51.5**：Then 无签发按钮，或点了说明找管理员；不产生钥匙。
- **GWT-51.8**：Then 只见前缀与状态（已签发 / 已吊销），不见明文。
- **GWT-51.9**：Then 无吊销控件，或点了说明找管理员。

edge-states：只读隐藏签发/吊销；超管无企业「出站拉数属于企业空间。」不是空表。列表失败 ≠ 空表。

## 自测命令

```bash
npm test --prefix frontend/admin -- --testPathPattern=OutboundKeys.test
npm run build --prefix frontend/admin
```

## 进度记录

| 时间 | 动作 | 说明 |
|---|---|---|
| | | |

## Evidence（交票前必填）

```
$ <命令>
<输出>
exit: 0
```

## 交票自检

- [ ] 产品名不是渠道组；入口不在 `/relay`
- [ ] 只读无写控件；再进页无明文
