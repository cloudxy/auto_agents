# T-19 · 治理收回技能/插件；目录不含已删行

> 泳道：L4｜**lane：api**｜并行组：**P0**｜角色：`/backend`｜粗估：1–2d｜状态：**todo**
> 上游：spec FR-88 · 命令收回已兑，本票技能/插件同构

## FR 锚点

| FR | GWT | 本票是否完整覆盖 |
|---|---|---|
| FR-88 | GWT-88.1, 88.2, 88.3, 88.4, 88.5 | 是 |

## 边界

**本票做什么**：源里已删技能/插件且同步结束 → 可操作列表无该行；公开商店也没有。与已兑命令收回同构。租户不能改收回。空目录总件不被 gone 行顶满。命令收回不得回潮。

**本票不做什么**：FR-91；公开种子谓词 → T-13（本票 gone 行，不是 nfr 种子）；不重排七叶。

**会改哪些文件**：

| 文件 | 改动性质 |
|---|---|
| `backend/services/power_market/sync.py` | 修改：技能/插件收回 |
| `backend/services/skill_import_service.py` | 对照/扩 |
| `backend/services/plugin_service.py` | 对照 |
| `backend/tests/test_t29_sources.py` | 修改 |
| `backend/tests/test_t30_command_cards.py` | 对照：命令回归 88.5 |
| `backend/tests/test_skill_import.py` | 扩 |

**不许改的文件**：公开种子谓词文件与 T-13 冲突时只加 gone 条件，不改 nfr 定义；frontend 七叶 IA。

## 依赖

| 前置票 | 需要它提供什么 | 状态 |
|---|---|---|
| — | 无 | — |

**并行组 P0。** 与 T-13 都动 power_market：T-13 读模型谓词，本票 sync 收回。可并行若不同函数；合入时 `list_public` 既滤种子又滤 gone。

## expand-contract

无新列。软删已有。

## GWT / Then（从 spec 抄）

- **GWT-88.1**：Then 该行不出现在可上架/可操作列表；公开商店也没有。
- **GWT-88.2**：Then 该行不出现在可操作列表；公开商店也没有。
- **GWT-88.3**：Then 「还没有目录项」；总件不被已收回行顶满。
- **GWT-88.4**：Then 拒绝；公开商店不变。
- **GWT-88.5**：Then 该命令仍不在可操作列表（已兑不得回潮）。

## 自测命令

```bash
uv run pytest -x -q backend/tests/test_t29_sources.py backend/tests/test_t30_command_cards.py backend/tests/test_skill_import.py
```

## 进度记录

| 时间 | 动作 | 说明 |
|---|---|---|
| | | |

## Evidence（交票前必填）

```
$ <命令>
<输出>
exit: 0
```

## 交票自检

- [ ] 技能+插件收回；命令不回潮
- [ ] 总件不被 gone 顶满
- [ ] 租户改收回拒绝
