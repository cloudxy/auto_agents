# 用户故事 · 四柱收口 + 不好用

> 对应 `spec.md` **v1.5**。L4 以 PRD 为准；本文是 3C 拆解，不是第二份合同。Confirmation 指向 spec 的 FR/GWT。
> 特征：`feat-product-complete`｜泳道：L4｜作者：pm 帽｜日期：2026-09-10｜修订：2026-09-11 增补周期 rework r2（并案 QA-31…39；GWT 区间同步 v1.5 新格）
> 禁止代选：Q-VOICE / Q-PRICE / Q-MARKET-USER / Q-AGPL / Q-OPS-COLLECT / Q-C-REG。
> 已关勿再问：Q-LLM / Q-BILL / Q-RELAY / Q-OPS-DUTY / Q-ADMIN-WAVE / Q-QUEUE-DEPTH / Q-OVERVIEW-3Q。
> v2 Wave 0+L+1 故事（US-W0-* / US-WL-* / US-W1-*）**已关账**，本文不再当作未完成。

## appetite

程序合计 **15–21 人周**（Wave C **6–10** · Wave U **4–7** · Wave A **5–8**；重叠，禁止加总各故事 Effort）。**停判线保持：architect 校正后 >21 人周停下重判**（Wave A 计入，不因增补抬高）。

## 来源

| 来源 | 证据 |
|---|---|
| 操作者约束 | `state.yaml`：一次做完未执行计划 + 深度梳理不好用 |
| 操作者反馈 2026-09-11 | `operator-feedback-2026-09-11.md` F1–F13（verbatim + file:line 核对）；Q-ADMIN-WAVE 已决 |
| ops 信号池 | S-01…S-11；S-08 强（400 张 NFR 卡 / 无翻页） |
| 诊断 | backend / frontend / designer / qa 2026-09-10 |
| 已关问 | Q-BILL 线下骨架；Q-RELAY 渠道组 SKU |

无客户工单。无出处的故事不进本文。

---

## US-C-01 满额走同一套下一步，并能看见自己的单

### Card

作为用量已经满额的租户负责人，我想官网和后台告诉我同一件「下一步」，提交线下申请之后还能在后台找到这张单，以便我知道有没有人在处理——而不是一边说买不了、一边说提交订单、提交完就消失。

### Context

| 项 | 内容 |
|---|---|
| 什么时候用 | token / 存储 / 并发顶到上限；或主动要升档 |
| 多频繁 | 每企少见，但卡一次就停工 |
| 这之前 | 看用量进度条变红或提交采集被拦 |
| 这之后 | 等管理员确认收款；打开「我的订单」能看见已确认 |
| 现在怎么做 | 官网「预告不可购买」；用量「本波不提供自助」与「提交升级订单」并排；`listMyOrders` 不渲染；只读看到内部码口吻 |
| 不做会怎样 | 继续两套真相；申请无法核对；只读被内部码赶走 |

### Confirmation

FR-50、FR-87。GWT-50.1…50.16；GWT-87.1…87.3。在线支付保持空（在线渠道 **不产生任何单**）。不到注册。不写成「当前可买」。**FR-50 完成停在「我的订单能看见已确认」。** 官网动词 / 确认后履约三数字 / 是否撤 ¥299 = Q-PRICE，本故事不代选。GWT-87.1 经办满额只见「已达配额上限」+「请联系企业管理员」。

---

## US-C-02 自己签发出站拉数钥匙

### Card

作为要把结果拉进自家系统的采集经办，我想在后台签发一把只属于我们公司的出站拉数钥匙，以便外部系统能拉到我们的条——别人的条拉不到，渠道组令牌也冒充不了这把钥匙。

### Context

| 项 | 内容 |
|---|---|
| 什么时候用 | 第一次要把结果接到内部仓库 / 脚本 |
| 多频繁 | 每企少次签发，之后反复调用 |
| 这之前 | 采集已经出数 |
| 这之后 | 用钥匙拉本企业结果 |
| 现在怎么做 | 出站 401；无签发入口；渠道组 `sk-` 令牌容易被当成这把钥匙 |
| 不做会怎样 | 外部拉数永远平台配置；两把钥匙混名 |

### Confirmation

FR-51。GWT-51.1…51.10。FR-13 未绑定拒绝保持。明文只一次；再进页只见前缀。经办能签发也能吊销；只读不能签发、不能吊销。出站拉数钥匙打渠道组 Base URL → 拒绝，不是套餐超限，用量不变。

---

## US-C-03 渠道组令牌能用，用量会动

### Card

作为租户负责人，我想签发渠道组令牌之后立刻知道 Base URL 和用法，并且真正打到平台网关后用量不是 0，以便按常见中转站那样把令牌交给同事用——我仍然改不了平台总闸。

### Context

| 项 | 内容 |
|---|---|
| 什么时候用 | 开通后要接客户端；或满额前要看消耗 |
| 多频繁 | 签发少次；看用量会反复 |
| 这之前 | 打开渠道组（菜单必须找得到，见 US-U-02） |
| 这之后 | 把令牌配进客户端；看 used |
| 现在怎么做 | 能建组能签发；无 Base URL；used 恒 0；令牌不打网关；官网企业档仍预告渠道组 |
| 不做会怎样 | SKU 是空壳；对外两套真相 |

### Confirmation

FR-60。GWT-60.1…60.11。Q-AGPL 未关：官网 0 次「当前可买中转」。官网渠道组句 vs 后台能否管理的「不得互否」**不是**本故事完成条件（等 Q-PRICE / Q-AGPL）。免费是否已有渠道组 / 是否 SKU = Q-PRICE / Q-AGPL，不代选。经办能看不能签渠道组令牌。A 调用不动 B 用量。失败≠空见 FR-84。GWT-82.1 / GWT-60.4 仍真（租户有渠道组），不加档位前提。

---

## US-C-04 值班看见伪装，渠道不自动关

### Card

作为平台值班超管，我想打开值班入口就看见「伪装」判定，以便决定怎么处理——系统不得因为伪装把渠道自动关掉。

### Context

| 项 | 内容 |
|---|---|
| 什么时候用 | 探针跑完一轮之后 |
| 多频繁 | 值班班次 |
| 现在怎么做 | 不熔断已兑；页上伪装可见性未按 Then 验收；空态已有网关两句 |
| 不做会怎样 | 熔断句在、值班当没看见 |

### Confirmation

FR-61。GWT-61.1…61.3。空态不另写第三套。入口可改名，同一入口。

---

## US-U-01 商店是商品，列表能翻页

### Card

作为第一次打开官网能力市场的访客，我想看到已上架的商品而不是测试卡；列表页大小不超过 20，并且只有货架真的超过 20 张时我才能翻到第 21 张——而不是对着 400 张 NFR 测试卡以为店没开张，也不是为了翻页再造 21 张商品。

### Context

| 项 | 内容 |
|---|---|
| 什么时候用 | 打开 `/capabilities` 或首页精选 |
| 多频繁 | 每名新访客 |
| 这之后 | 打开详情或离开 |
| 现在怎么做 | total=400 全 `nfr01qc2-*`；无翻页；四类 0；夹具搜 pdf 0 命中 |
| 不做会怎样 | 继续演示污染；D4 被种子当供给 |

### Confirmation

FR-80、FR-81、FR-92（`market_list_paged`，含 `anonymous_id`）。页大小 ≤20；当且仅当非种子 listed>20 时第 21 张可到（≥21 用夹具）。不重开获客模型。失败≠空已兑路径保持。

---

## US-U-02 侧栏是我能做的事

### Card

作为已登录租户，我想侧栏里的叶都是我能做完的事，以便找到渠道组和我的安装——不要走进角色权限/企业管理。

### Context

| 项 | 内容 |
|---|---|
| 什么时候用 | 每次登录后找功能 |
| 多频繁 | 每天 |
| 现在怎么做 | `/auth/menus` 与静态树两套真相；幽灵组织页租户进得去 |
| 不做会怎样 | 以为产品没有渠道组；或在别人的公司列表里点写 |

### Confirmation

FR-82。五组不重排。直打组织页 = 页面不存在同形。FR-91 / 侧栏「能力市场」不得进七叶 **本轮不在本故事**（等 Q-MARKET-USER；不代选货架或安装）。

---

## US-U-03 用注册邮箱能进后台

### Card

作为刚填完管理员邮箱办好企业的负责人，我想用这个邮箱和密码登录进自家后台，以便当天开始第一次采集——失败时不要跟我假装是密码错了。

### Context

| 项 | 内容 |
|---|---|
| 什么时候用 | 注册成功当次 |
| 多频繁 | 每家企业一次（之后也会用同一标识） |
| 这之前 | 官网注册表只收邮箱 |
| 这之后 | 仪表盘 / 采集 |
| 现在怎么做 | 登录要用户名=邮箱 @ 前一段；填邮箱 → 与错密码同一句 |
| 不做会怎样 | D2 登录率结构为零 |

### Confirmation

FR-83。GWT-83.1…83.5。到期句与错密码不同句（已兑 FR-08）。未知标识与错密码同句；不设「标识填错」第三句。

---

## US-U-04 失败不是空，没工人有下一步

### Card

作为第一次出数的经办，我想分得清「接口失败」「真的还没有」「现在没有工人」，以便决定是重试、去采集、还是去节点——不要一张空表三种原因。

### Context

| 项 | 内容 |
|---|---|
| 什么时候用 | 打开节点 / 仪表盘 / 数据 / 渠道组 / LLM / 任务 |
| 多频繁 | 开工第一天最高 |
| 现在怎么做 | 失败 catch 成空表或 0；无工人横幅吓人；弹窗仍 toast「正在排队」 |
| 不做会怎样 | 以为系统坏了或以为已经在爬 |

### Confirmation

FR-84、FR-85。GWT-84.1…84.4；GWT-85.1…85.3。不重开出数环。GWT-85.2 只冻拦住，不验收「提交后立即可见」。

---

## US-U-05 只读看不见写；设置不能谎称改了官网

### Card

作为只读成员，我想成员页没有我点了也不会成功的写按钮，以免以为我能加人。作为公司管理员，我不想保存设置后被通知「官网已同步」，而访客看到的首页根本没变。

### Context

| 项 | 内容 |
|---|---|
| 什么时候用 | 打开成员；打开系统设置 |
| 现在怎么做 | viewer 看见添加/改角色/删除；设置 toast「官网内容已实时同步更新」；Home 写死 |
| 不做会怎样 | 权限靠 toast；对外说谎 |

### Confirmation

FR-89、FR-90。GWT-89.1…89.3；GWT-90.1…90.3。不在本故事做「真的同步官网」。

---

## US-U-06 治理目录不要把已删的源文件还摆在台上

### Card

作为平台超管，我想同步跑完以后，源里已经删掉的技能/插件不要还占着治理目录，以便上架列表是现在还存在的资产。

### Context

| 项 | 内容 |
|---|---|
| 什么时候用 | 源同步之后逛目录 |
| 现在怎么做 | 命令会收回；技能/插件不会；列表不过滤已收回行 |
| 不做会怎样 | 目录被 gone 行顶满；公开/治理两套「还在不在」 |

### Confirmation

FR-88。GWT-88.1…88.5。租户不能改收回。

---

## US-A-01 找回删错的用户

### Card

作为平台超管，我想在用户管理里筛出「已删除」的用户，并把标识没被占用的恢复回来，以便删错了能自救——而不是软删即永别、username/email 还被永久占着。

### Context

| 项 | 内容 |
|---|---|
| 什么时候用 | 误删后发现；离职员工回归 |
| 多频繁 | 少见，但一旦发生没有出路 |
| 现在怎么做 | 无「已删除」筛选、无恢复入口；软删行永久占用 username/email（想重建同名用户也不行） |
| 不做会怎样 | 误删只能手工改库；标识被死行占死 |

### Confirmation

FR-93。GWT-93.1…93.9。恢复仅平台超管；默认视图不见已删（既有行为）；占用冲突=保持已删除+中文说明（username 按同租户判、email 按全局判，GWT-93.4）；占用可释放按 username/email **各自独立拆格**（GWT-93.5/93.7）；再恢复（GWT-93.8）与重复恢复（GWT-93.9）独立成格；上报 `user_restored`（GWT-92.8，重复恢复不重复上报）。

---

## US-A-02 管企业不弄坏基座

### Card

作为平台超管，我想在企业管理里改企业名、停用/启用企业账户，并一眼认出哪个是平台默认租户，以便控制企业账户——而 admin 账号和「平台租户」我永远弄不坏，也不会再把站点名 AutoAgents 当成归属公司。

### Context

| 项 | 内容 |
|---|---|
| 什么时候用 | 企业要求改名；控制异常企业 |
| 现在怎么做 | 企业管理页只有新建+列表；停用藏在运营台；无改名；「平台租户」混在普通企业里认不出 |
| 不做会怎样 | 控不住企业账户；误改平台基座 |

### Confirmation

FR-94、FR-95。GWT-94.1…94.5；GWT-95.1…95.7。平台租户不可改名/删/停用；admin 不可删（即使有第二个超管）；企业删除不开（§5）；命名与归属收口见 spec §0.4；个人注册=Q-C-REG 不代选。

---

## US-A-03 点菜单不闪、页头不占屏

### Card

作为总台使用者，我想点侧栏菜单时侧栏原地不动、页名只在顶栏出现一次，以便内容区大、注意力在业务上——不要每切一页侧栏整树闪一次「权限加载中」，也不要三层标题把第一屏吃掉。

### Context

| 项 | 内容 |
|---|---|
| 什么时候用 | 每次在总台导航 |
| 多频繁 | 全天高频 |
| 现在怎么做 | 平台页独立布局分支，跨组切换整树卸载重挂；顶栏 pageTitle+页内标题卡+横幅三层重复 |
| 不做会怎样 | 总台操作节奏被打断；主要内容不凸显 |

### Confirmation

FR-96、FR-99。GWT-96.1…96.4；GWT-99.1…99.4。FR-82 菜单真相不变（GWT-96.4 钉住）；首次加载允许「权限加载中」；失败句与写控件隐藏不因布局精简回退（GWT-99.2/99.4）。

---

## US-A-04 一眼知道哪个模型是默认

### Card

作为配置大模型的总台使用者，我想页上写的是「默认」而不是「激活」，并说明「未指定模型时默认用它、可按需更换」，以便我理解选路规则——后端本来就是这么工作的，只是文案骗了我。

### Context

| 项 | 内容 |
|---|---|
| 什么时候用 | 初次配置供应商；更换默认模型 |
| 现在怎么做 | 「激活」误导为开关；后端实为租户域互斥默认+三段选路 |
| 不做会怎样 | 继续误解「激活=唯一在跑」 |

### Confirmation

FR-97。GWT-97.1…97.4。同域至多一个默认；选路行为不变（不重做数据面）；空态/失败句与 FR-84 同一套。

---

## US-A-05 总览一屏回答值班三问

### Card

作为值班超管，我想打开中转站总览就回答三问——网关能不能用、渠道真不真稳不稳、刚才发生了什么——并能点「立即探测」、从事件跳转，以便值班有抓手；总览/探针/事件三个 tab 与欢迎语同一行，不再被标题卡横幅挤掉半屏。

### Context

| 项 | 内容 |
|---|---|
| 什么时候用 | 值班巡检；渠道疑似异常时 |
| 现在怎么做 | 三 tab 数据都真但没有值班视角；探针只有定时循环没有手动；问题渠道不置顶；页头占屏 |
| 不做会怎样 | 有数据看不见；出事要挨个 tab 翻 |

### Confirmation

FR-98（FR-61 呈现级扩展，不另开第二入口/第二套空态）。GWT-98.1…98.7。网关不可达走已冻空态句；伪装不熔断保持；租户无入口（GWT-61.3 同句）；视觉细节归 designer。

---

## US-A-06 本地资产一键进目录，专家团组装智能体

### Card

作为平台超管，我想从本地文件或目录一键导入 skill/agent/command/plugin 四类资产，组建专家团时成员可以直接选智能体，以便把手上的资产搬进平台组装起来——不用先传 GitHub 再走 URL 导入。

### Context

| 项 | 内容 |
|---|---|
| 什么时候用 | 批量接入本地积累的技能/插件/命令/智能体 |
| 现在怎么做 | 仅 URL 导入+服务器目录扫描；agents 无扫描通道；团队成员只能选 expert |
| 不做会怎样 | 资产进不来；「组装智能体」落空 |

### Confirmation

FR-100、FR-101。GWT-100.1…100.8；GWT-101.1…101.5。导入未上架、不自动进公开商店；目录导入部分成功列原因；超大文件拒绝含上限数字；路径逃逸拒绝且资产目录外零新文件（GWT-100.7）；幂等重导不产生第二行（GWT-100.8）；上报 `asset_imported`（GWT-92.9）；成员域=专家+智能体、组长仍专家；不引入执行引擎。

---

## US-A-07 平台超管自己能跑 AI 采集

### Card

作为平台超管，我想用 AI 采集规划时以「平台租户」身份试采和上线，以便自己先跑通再给别人用——不要被「没有企业身份，无法入队」挡在门外。

### Context

| 项 | 内容 |
|---|---|
| 什么时候用 | 验证 AI 规划链路；演示 |
| 现在怎么做 | 平台超管 actor 租户为 None，试采/上线直接被拒 |
| 不做会怎样 | 超管无法自测；链路问题靠租户反馈 |

### Confirmation

FR-102。GWT-102.1…102.5。任务与结果归属平台租户；不绕过配额（满额=中文句）；不入 WACT/PC（蓝图排除）；租户归属语义不变（GWT-102.4）。

---

## US-A-08 采集任务栏目都是采集的事

### Card

作为经办，我想采集方案能改参数、能删除，方案列表里没有 demo 爬虫和源码文件清单，告警规则没有「配了不生效」的死类型，以便栏目里看到的全是能用的采集资产。

### Context

| 项 | 内容 |
|---|---|
| 什么时候用 | 维护采集方案；配置告警 |
| 现在怎么做 | 方案只能改标题/描述；列表混入 example/openweather/skill_harvester/flow 伪爬虫/源码清单；queue_depth 是死规则 |
| 不做会怎样 | 方案错了只能重建；列表被噪声占满；告警形同虚设 |

### Confirmation

FR-103、FR-104、FR-105。GWT-103.1…103.5；GWT-104.1…104.4；GWT-105.1…105.4（**接通已决**，Q-QUEUE-DEPTH，2026-09-11；GWT-105.1 为唯一正常路径，GWT-105.2 下架支作废见 spec §5；不留第三态）。编辑对后续任务生效；被引用删除拒绝；「任务模板」隐藏 tab 维持现状。

---

## 约束（非功能）

| 类别 | 要求 |
|---|---|
| 性能 | 市场列表/翻页卡片可见 P95 < 2s（≤400 张非种子） |
| 权限 | 矩阵见 spec §4；直打平台专属与组织页 = 页面不存在同形 |
| 安全 | 明文钥匙只一次；事件不含明文；用户可见禁止内部码 |
| 合规 | 0 次「当前可买中转」；不发明电话 |
| 容量 | 市场第一页 ≤20；当且仅当非种子 listed>20 时第 21 张可到（夹具，不暗含再造商品） |

## 范围外

见 spec §5。故事不承担：在线支付、改 Slogan、获客模型、焊网关、alembic 漂移、打开 LLM 闸、C 端个人注册（Q-C-REG）、企业删除、专家团执行引擎、导入自动上架。

## 度量

| 项 | 内容 |
|---|---|
| 怎么知道有用 | 仍用 WACT；本波用蓝图 PC-1…PC-4（注册邮箱登录率 / 商店种子曝光 0 / 线下单可自查 / 令牌打通率）解释 WACT=0。PC-3 **无** 24h 打开窗口。Wave A 不新增驱动指标：判定 = 护栏 0 + 抽检（蓝图 §4 追加行） |
| 数据来源 | 已冻 FR-15 + 本波 FR-92（含 `user_restored` / `asset_imported`）；平台租户（slug=platform）不计入 WACT / PC |
| 不能变差的 | 跨租户 0；内码抽检 0；失败装空 0；两套真相 0；可买中转 0；平台租户/admin 基座破坏 0；方案视图混入 0；告警死规则 0 |

口径以 `metrics-blueprint.md` 为准。
