# 前端诊断 · feat-product-complete（定义帽 / frontend）

| 字段 | 值 |
|------|----|
| 角色 | frontend（只读诊断，不写产品 UI / 不定 tokens / 不写 API / 不代选 Q） |
| 泳道 | L4 · `feat-product-complete`（对照冻结 `feat-four-pillars-v2` FR-01…20 / 70…75 / 30…45 + 018c369 账务/渠道组骨架） |
| 范围 | `frontend/admin/src` · `frontend/official/src` |
| 拒绝 | 不定 tokens（→ designer）· 不写 API/schema（→ backend）· 不写实现票 · 不代选 Q-VOICE / Q-PRICE / Q-MARKET-USER / Q-AGPL |
| 活体 | 2026-09-10 Chrome `--dump-dom` + curl。9112/9113/9111 **在听**。租户登录后后台页 **未获取活体会话**（无注入凭证），以源码+单测+匿名壳为准 |

**一句话：** Wave 0/1 诚实句与壳（定价分档、注册去登录、平台 404 同形、七叶、商店失败≠空）已经落地；现网「不好用」集中在 **商店被 400 张 NFR 种子淹没且无翻页**、**租户后台仍把治理台当货架**、**018c369 渠道组/升级订单是能点的骨架但完不成中转站动作**、以及多处 **失败装空 / 写控件不对角色隐藏**。Wave 2/3 未履约的只剩出站钥匙（FR-51）与在线支付；`/relay` 与线下订单 **不是 stub，是难用的已落地骨架**。

分类只用三档：**故障**（与冻结 FR/六态相反，或失败=空、或文案说谎）· **难用**（已实现但角色走不完）· **stub**（合同明确本波不做）。数据面污染标在故障旁注，不另开第四档。

---

## 0. 活体（9112 / 9113 / 9111）

| 路径 | 结果 |
|------|------|
| `http://127.0.0.1:9113/` | 200。Hero「智能数据采集交给 AI」；无 `128,000+` / Excel；精选 6 张全是 `NFR卡片399…394` +「评审中」 |
| `/pricing` | 免费档 5 / 10,000 / 20 万 +「免费注册」；专业/企业「预告不可购买」；企业档仍印「中转站渠道组分配 预告」 |
| `/register` | 企业名 / 管理员邮箱 / 密码 /「创建企业」。套在官网 Header+Footer 里 |
| `/capabilities` | 五类 Tab 在；**无翻页、无 total**；可见 20 张 `NFR卡片380–399` 全「评审中」；列表无「订阅」 |
| `/skills` | 到达市场（与 `/capabilities` 同壳）；活体仍是那 20 张 NFR 技能卡 |
| `/capabilities?type=bogus` | 「没有这种类型」+ 五类说明（Tab 被藏，无「回全部」按钮） |
| `/capabilities/skill/example-pdf-extractor` | 官网 404「页面不存在，可能已被移除或地址有误」+「返回首页」 |
| `/not-a-real-page` | 与上句同形 |
| API `GET /api/v1/public/capabilities?page_size=1` | `total=400`，`has_more=true`；plugin/command/agent/team **各 0**；`q=pdf` **0** |
| `GET /api/v1/billing/plans` | 匿名 200（免费/专业档在库；前端定价页不读此接口） |
| 9112 `/login` | 登录壳 +「没有账号？企业注册」 |
| 9112 匿名 `/dashboard` `/relay` | 跳登录 |
| 9112 匿名 `/newapi` `/platform-ops` | **404 同形**「页面不存在或已被移除」+「返回工作台」（无「抱歉」） |
| 9112 登录后 `/usage` `/relay` `/capabilities` `/platform-ops` | **未获取活体会话** |

---

## 1. 对照冻结 FR 与 018c369（只记界面能单独证伪的）

塑形站点图（`02-shape/sitemap.md`）仍写「不做 `/relay`」。操作者已关 **Q-RELAY**，018c369 把租户渠道组 SKU 和线下订单做成现网。本诊断以 **state.yaml 已关问 + 现网代码** 为准，站点图那句视为过期，不把 `/relay` 再标 stub。

| 面 | 冻结要求 | 现网前端 | 判 |
|----|----------|----------|----|
| FR-01/02/03 官网诚实 | 闭集 A 可走完；无虚构规模；导出 CSV/JSON+100 条 | 活体首页/定价满足闭集 A 数字与无 Excel/无大数 | **满足**（首页架构/告警区仍有过承诺，见 P-OFF-04） |
| FR-04/05 | 注册主按钮去登录；付费档不到注册 | Register 成功链 `/login?from=/dashboard`；定价付费「预告不可购买」 | **满足**（注册→登录用户名落差见 P-OFF-07） |
| FR-06/07/17 壳 | 平台页隐藏；直打 404 同形；空缓存非全开写 | `PlatformAdminLayout` + `platformOnly`；匿名/租户打 `/newapi` `/platform-ops` 活体 404 | **壳满足**；`/settings` 仍是租户 admin 可进的「改官网」入口（P-ADM-12） |
| FR-12/16/50 用量 | 满额可理解句；CTA 不到注册；将满警告 | 用量有将满/满额句、mailto、**另有「提交升级订单」** | 闸文案 **满足**；双出口 **难用**（P-BILL-01） |
| FR-18/19 出数 | 无工人拦住或立即可见；0 条≠还在跑 | 任务页 Alert「采集未运行，不会出数」；结果列「真的 0 条」/「还在跑」 | 词在，**下一步与拦截不足**（P-COL-01） |
| FR-30…33 商店 | 五类；失败≠空；分页可见 | 五类 Tab+失败句单测在；活体 400 条无翻页；四类库存 0；夹具详情 404 | **合同句在、现网不可逛**（P-MKT-01） |
| FR-34/35 订/装 | 详情订；我的安装空/失败分开 | 官网详情「登录后订阅」；`MyInstalls` 空/失败分句 | **代码满足**；活体详情被种子挡住 |
| FR-37 七叶 | 源\|目录\|插件\|技能\|命令\|智能体\|专家团 | admin `/capabilities` 七叶在 | **超管满足**；租户也看见治理台（P-GOV-01） |
| FR-41 第一方夹具 | `example-pdf-extractor` 可搜可订 | 活体公开列表 0 命中、详情 404 | **现网不满足**（数据+精选被 NFR 种子占满） |
| FR-51 出站钥匙 | 本程序无自助签发拉数钥匙 | 无该入口 | **stub，符合** |
| FR-60 渠道组可见性 | Q-RELAY 未关则无「我的渠道组」 | Q-RELAY **已关**；菜单 `/relay`「渠道组」 | 不再是 stub；**骨架难用**（P-REL-01） |
| 018c369 待确认收款 | 超管确认线下单 | `PlatformOps` Tab「待确认收款」 | 入口在；空/错/金额形态差（P-BILL-02） |

---

## 2. 现状地图（2026-09-10，不是 09-08 旧诊断）

### 2.1 官网 `frontend/official`（9113）

路由：`App.tsx` — `/` `/capabilities` `/capabilities/:type/:slug` `/skills`（同市场） `/register` `/pricing` `*`。

导航 `SiteLayout.tsx` NAV_LINKS：能力市场 / 定价 / **注册**（站点图把注册放页脚/次要，现网顶栏一级）。

无会话。无 `ErrorBoundary`。

### 2.2 后台 `frontend/admin`（9112）

五组仍冻结。叶相对站点图 **多了一片**：概览 → **渠道组 `/relay`**（`tenantOnly` + `menu:relay`）。

平台写叶 `/newapi` `/platform-ops` `/users`：导航 `platformOnly`；直打 `PlatformAdminLayout` → 后台 `NotFound`。

仍在路由、不在菜单：`/enterprise` `/rbac`（X-IA：幽灵不修）。`/unauthorized` 仍服务 `requireAdmin` 失败（设置/幽灵），文案「抱歉，您没有权限」——**不得**用于平台写叶（现已不用）。

`AdminLayout` 优先 `/auth/menus` 动态树，空才回退 `menuConfig`。只改静态树不够。

---

## 3. 痛点（谁 / 在哪 / 做什么失败 / 空错态 / 分类）

### 3.1 官网访客 · 首屏 / 定价 / 注册

| ID | 谁 | 在哪 | 做什么失败 | 空/错态是否可理解 | 分类 |
|----|----|------|------------|-------------------|------|
| **P-OFF-01** | 匿名访客 | 活体 `/` 能力精选 · `SkillsSection.tsx:26-33,79-118` | 想来「已上架可复用能力」，看到 6 张 `NFR卡片` +「评审中」。卡片点进 `?type=skill&q=nfr01qc2-…` 不是产品夹具 | 空态句本身正确；**现网把种子当精选**，看起来像「市场没准备好」 | 故障（数据污染渲染成货架） |
| **P-OFF-02** | 匿名访客 | `/` Hero `Home.tsx:91-122`；Q-VOICE 未关 | 读完第一句仍不知道产品是采集工作台、SaaS、中转还是市场。芯片「分布式 Worker 协同」无对应访客动作 | 无错态 | 难用（Q-VOICE 未关，不代选） |
| **P-OFF-03** | 匿名访客 | `/` CTA `Home.tsx:197-207` + 定价 | 「专业档与企业档尚未开通购买」与用量页「提交升级订单」、后台 `/relay` 并存。访客以为渠道组买不到，租户进去却有「新建渠道组」 | 无错态，**两套事实** | 难用（Q-PRICE 未关；018c369 已让后台可下单） |
| **P-OFF-04** | 匿名访客 | `FeaturesSection.tsx:59-62`「Webhook 多通道通知」；`ArchitectureSection.tsx:30-55`「RBAC / 横向弹性扩容 / 导出中心」；`AiFlowSection.tsx:54`「一键上线…数据持续稳定入库」 | 闭集 A 以外的「现在就能用」口吻。访客按四步走，工人不在线/无模型时会被 FR-18/70 拦住，首页没预告这闸 | 首页无这些失败态 | 难用（过承诺；未标预告） |
| **P-OFF-05** | 匿名访客 | `SiteLayout.tsx:20-24` 顶栏「注册」与 Hero「免费注册」并列；`Register.tsx` 又是 `minHeight:100vh` 套在 Header+Footer 里 | 注册页双层壳，小屏要滚才能看到表单 | 无 | 难用 |
| **P-OFF-06** | 刚注册负责人 | `Register.tsx:207-232` 填 **管理员邮箱**；登录页 `Login.tsx:167-176` 要 **用户名**；后端 `username=email.split('@')[0]` | 用邮箱去登录失败（AUTH_FAILED →「用户名或密码不正确」）。成功屏只报负责人用户名，表单未告诉「登录名是邮箱 @ 前一段」。两家 `admin@…` 会撞名 | 错态可理解但 **归因错误**（像密码错） | 故障（注册→登录断链变种） |
| **P-OFF-07** | 付费意向访客 | `Pricing.tsx:11,152-176` `CONTACT_MAIL` 默认 `contact@localhost` | 点「预告不可购买」→「联系平台」；mailto 打不开时复制 localhost | 错态句按设计有；**联系人不可达**（Q-OPS-COLLECT 未关，不代选） | 难用 |

定价闭集 B（工单/私有库/客户成功/渠道组）均带「预告」，主按钮不到注册 —— **FR-01/05 句满足**。渠道组在 Q-RELAY 已关后仍标预告，与 `/relay` 打架，归 P-OFF-03，不另开。

### 3.2 官网访客 · 能力市场

| ID | 谁 | 在哪 | 做什么失败 | 空/错态 | 分类 |
|----|----|------|------------|---------|------|
| **P-MKT-01** | 访客 | `Capabilities.tsx` 列表 **无 `page`/`total` UI**（service 默认 `page_size=20`）；活体 `total=400` | 以为店里 20 件。第 21 件以后不可达。NFR-02「必须分页、用户一次看不到全表」——用户一次看不到全表是真的，但 **也看不到还有下一页** | 非空故不走空态；失败句单测在、活体未触发 | 故障（相对 NFR-02 / GWT-33.4 的「还有下一页」） |
| **P-MKT-02** | 访客 | 活体四类库存 0；筛「插件/命令/智能体/专家团」 | 走筛选空「没有符合条件的能力」+「清除筛选」 | **可理解**，但五类货架名不副实 | 难用（库存；前端仍渲染四类 Tab） |
| **P-MKT-03** | 访客 / 经办 | 活体 `/capabilities/skill/example-pdf-extractor`；`q=pdf` 0 条 | FR-41 夹具应可搜可订。现网 404 与真缺页同形——对未上架是对的，对「商店闸已开」的夹具是 **货没了** | 404 文案可理解，**原因不可区分**（未上架 vs 种子冲掉） | 故障（相对 FR-41 现网） |
| **P-MKT-04** | 访客 | 每张卡 `Capabilities.tsx:101`「评审中」（`score==null`） | 400 张全评审中，像未过审。GWT-01.6 禁「认证」句，「评审中」擦边 | 非错态 | 难用 |
| **P-MKT-05** | 访客 | `Capabilities.tsx:240-244` 非法类型藏掉 Tabs | 看到「没有这种类型」，没有「回全部」控件，只能改 URL | 错态句正确，**恢复动作弱** | 难用 |
| **P-MKT-06** | 未登录访客 | 列表卡无订阅；详情 `CapabilityDetail.tsx:25-31,93-102`「登录后订阅」跳 **后台** `/login?from=/capabilities?subscribeType=` | 逛的是官网详情，回来落在 **后台七叶治理台** 并弹出订阅窗，不是原详情。`REACT_APP_ADMIN_URL` 空则按钮消失（有单测） | 加载失败有「重试」；404 同形正确 | 难用（会话落点已冻「订在后台」，跳转无说明） |
| **P-MKT-07** | 访客 | 精选走 `listPublicSkills`（`SkillsSection.tsx:12,29`），市场走 `listPublicAssets` | 两套公开列表。闸不一致时首页与市场货不同 | 失败句首页「暂时无法加载能力」+重试 **可理解** | 难用（双 API） |

商店失败≠空、预告无按钮、`/skills` 映射、非法类型不兜技能 —— **代码合同句在**。活体被 NFR 种子与无翻页打穿。

### 3.3 租户经办 / 负责人 · 采集出数

| ID | 谁 | 在哪 | 做什么失败 | 空/错态 | 分类 |
|----|----|------|------------|---------|------|
| **P-COL-01** | 经办 | `Spiders.tsx:182-188` + `copy.ts:2`；`TaskModal.tsx:73-92` **不读** `workerOffline` | 无工人时页顶一句「采集未运行，不会出数」，**无「去节点 / 联系值班」**。弹窗仍可提交，成功 toast「正在排队执行」，随后任务挂起 | Alert 可理解但 **不像拦住**；与 GWT-18.2「拦住或立即可见+下一步」差下一步 | 难用（词在，动作不完整） |
| **P-COL-02** | 经办 | `TaskList.tsx` 无 `emptyText`；`ResultDrawer.tsx:187-190`「真的 0 条」/「还在跑」 | 零任务表走 antd 默认空。0 条结果用内部验收词，经办要猜 | 弱 | 难用 |
| **P-COL-03** | 经办 | `Nodes.tsx:33-39,97` `useQuery` **无 isError 分支** | 节点接口失败时表格「暂无在线节点（心跳…）」—— **失败=没工人** | 不可区分 | 故障（失败装空） |
| **P-COL-04** | 经办 | `Dashboard.tsx:65-73,146-194` | 统计失败 toast + 卡片 **0**。零任务引导第 1 步 `/llm`（允许）但不提工人/渠道组 | 失败不像失败 | 故障（失败装 0） |
| **P-COL-05** | 经办 | `Data.tsx:97-104,251-270` 统计失败同样 0；结果失败 toast，表空 | 「还没有采集结果」与加载失败可混 | 部分 toast | 故障（失败装空） |
| **P-COL-06** | 经办 | `AiPlans.tsx` + `useAiPlanFlow.ts:134-136` 规划失败只 `apiErrorMessage` | 套餐满 / 网关挂 / 没模型 三条冻结句（FR-12/70/74）若后端 message 不干净，可能漏内部码（`errors.ts:28-30` 透传 `message`） | 无页级三态分流 | 难用 |
| **P-COL-07** | 只读 | 任务创建靠 `btn:create` 隐藏；Members **不**按角色藏写按钮 | 采集写还算藏了；成员页不是（P-ADM-11） | — | 见下 |

### 3.4 租户 · 用量 / 018c369 升级订单

| ID | 谁 | 在哪 | 做什么失败 | 空/错态 | 分类 |
|----|----|------|------------|---------|------|
| **P-BILL-01** | 负责人（token 满） | `Usage.tsx:56-71,139-165,211-221` | 同时出现「申请提升配额」（mailto `contact@localhost`）和「提交升级订单」。`listMyOrders` **只在测试 mock，页面不渲染**。提交成功一句 toast「等待人工确认收款」，之后用量页找不到单 | 加载失败 Alert **无重试**（`Usage.tsx:114-116`）；平台超管无企业走「用量属于企业空间」正确 | 难用（骨架能点，闭环看不见） |
| **P-BILL-02** | 平台超管 | `PlatformOps.tsx:194-223` Tab「待确认收款」 | 表头「金额（分）」要心算；无租户名/企业 slug；`confirmOrder(r.id).then(load)` **无 catch**；空表 antd 默认「暂无数据」；无分页 | 加载失败仅 toast，表空 | 难用 + 失败装空 |
| **P-BILL-03** | 只读成员 | `Usage.tsx:47,127-129,153` | 只读条在；「提交升级订单」对 viewer 藏了；「申请提升配额」仍可开 mailto | 可理解 | 难用（轻） |
| **P-BILL-04** | 并发打满 | `Usage.tsx:108,139-165` | `anyFull` 含任务并发，但 CTA 只处理 token/存储。并发满只有「已达配额上限」无下一步 | 半句 | 难用 |
| **P-BILL-05** | — | FR-51 | 经办找「自己签发拉数钥匙」 | 无入口 | **stub，符合 GWT-51.1** |

在线支付未开通 —— **stub，符合 Q-BILL**。不要把线下订单入口再标成未实现。

### 3.5 租户 · 渠道组 `/relay`（018c369，Q-RELAY 已关）

| ID | 谁 | 在哪 | 做什么失败 | 空/错态 | 分类 |
|----|----|------|------------|---------|------|
| **P-REL-01** | 负责人 | `RelayGroups.tsx` 整页；菜单 `menuConfig.tsx:47` | 能建组、签发令牌、看前缀。**没有**：网关 Base URL、如何把令牌接到 LiteLLM/客户端、组与令牌的关联列、RPM/TPM 是什么。签发后 Modal「请立即复制」——这是唯一一次明文 | 加载失败 **toast**，两表变空（`32-43`）= 失败装空。空组无行动空态（默认「暂无数据」） | 难用 + 故障（失败装空） |
| **P-REL-02** | 经办 operator | `RelayGroups.tsx:21-22` `canWrite = isAdmin \|\| role==='admin'`；但 `_ROLE_PERMISSIONS` operator/viewer **都有** `menu:relay`（`auth.py:102-107`） | 菜单进得去，**没有新建/签发**。经办是中转站主力角色，只能看空表 | 无「当前账号不能签发，请联系企业管理员」 | 难用（权限隐藏不对称） |
| **P-REL-03** | 经办 | 吊销 `RelayGroups.tsx:127` 无确认；启停组无确认 | 误点即吊销 | 无 | 难用 |
| **P-REL-04** | 访客 | 官网定价企业档仍「中转站渠道组分配 预告」 | 与租户已有 `/relay` 冲突 | — | 同 P-OFF-03 |
| **P-REL-05** | 超管 | 页内 Alert 指向「中转站管控」；租户 **看不见** 该叶（正确） | 租户读这句会去找一个 404 页 | 直打 `/newapi` 404 同形正确 | 难用（文案对错角色） |

GWT-70.4 / 测试 `App.test.tsx:96-135` 禁止「我的中转令牌」「直连平台网关」字面 —— 页上用「签发令牌」，测试不炸，用户仍发得出密钥。这是 **产品已决 Q-RELAY 后的骨架**，不是 Wave 3 stub。

动态菜单：若 `/auth/menus` 有数据但未含 `/relay`，静态 `menuConfig` 不会出现。深链 `/relay` 仍进页。活体登录态未验菜单树。

### 3.6 租户 · 能力市场后台 / 我的安装 / 治理台

| ID | 谁 | 在哪 | 做什么失败 | 空/错态 | 分类 |
|----|----|------|------------|---------|------|
| **P-GOV-01** | 经办 | admin `/capabilities` `Capabilities.tsx:46-77` 七叶；站点图「租户：不把治理当货架」 | 侧栏叶名也叫「能力市场」，进去是源/目录/上架开关。源叶 `SourceTab.tsx:27-30,74-87`：非超管不拉源，空态「还没有源。登记源后才能同步。」—— **租户登记不了** | 空态教错角色 | 难用 |
| **P-GOV-02** | 超管/经办 | 七叶 `useState`，**不进 URL** | 刷新丢叶。窄屏靠 antd more，合同要求滚动不删叶（`more={{ icon: null }}`） | — | 难用 |
| **P-GOV-03** | 超管 | `Skills.tsx:239-247` 技能叶内再套「技能库 / 适配器矩阵 / 候选审核」 | 每天一次任务深度 3。候选空态仍「运行 skill_harvester」 | 工程师口吻 | 难用 |
| **P-GOV-04** | 超管 | `TeamLeafTab.tsx:10,22` `listAssets('expert')` | 组建专家团成员下拉走旧枚举，失败 catch→`[]`，表单像「没有智能体」 | 失败装空 | 故障 |
| **P-GOV-05** | 经办 | `ListingControls.tsx:53-62` 禁用+「需要平台市场权限」 | 符合 GWT-37.3。整列 Radio 仍占目录表，租户会去点 | 旁注可理解 | 难用 |
| **P-GOV-06** | 经办 | `MyInstalls.tsx` 空/失败/下架残留 | 空「还没有订阅的能力」+ 去官网市场；失败+重试 | **可理解**（对照良好） | — |
| **P-GOV-07** | 经办 | 安装表「启用/信任」无「这不写宿主」行内说明（只有信任确认 `installsCopy.ts:11`） | 打开启用以为本机生效 | 部分 | 难用 |

### 3.7 权限隐藏 / 幽灵 / 设置

| ID | 谁 | 在哪 | 做什么失败 | 空/错态 | 分类 |
|----|----|------|------------|---------|------|
| **P-ADM-10** | 权限未就绪 | `AdminLayout.tsx:65-68` `usePermission.ts:85-89` | 「权限加载中」；平台写叶 strip。符合 FR-17 | 可理解 | — |
| **P-ADM-11** | 只读 `viewer`（`menu:members` 在 `auth.py:101`） | `Members.tsx:176-179` **无** `usePermission` 藏「添加成员」；角色 Select/重置/删除对非 owner 全开 | 点创建 → 后端拒绝 toast。角色标签英文 `admin/operator/viewer`。密码校验 **min 6**，注册是 **8** | 错态不预解释 | 故障（写控件未按只读藏）+ 难用 |
| **P-ADM-12** | 租户公司管理员 | `App.tsx:123-129` `/settings` `requireAdmin`；`Settings.tsx:70-111` | GWT-01.5「寻找编辑官网卖点：没有该入口」。本页 label「展示在官网 Hero Section 的副标题」，保存 toast「官网内容已实时同步更新」。官网 `Home.tsx` **写死** `SITE_NAME`/`Hero`，不同步 | 成功态 **说谎** | 故障 |
| **P-ADM-13** | 经办 | `/settings` → `Unauthorized.tsx:15`「抱歉，您没有权限」 | 平台写叶已改 404 同形；本页仍道歉 403。两套权限语言 | 可理解但与 FR-07 词表混用 | 难用 |
| **P-ADM-14** | 任何人 | `App.tsx:99-114` `/enterprise` `/rbac` | 菜单无（符合 X-IA 不修幽灵）。租户 admin 深链仍进整页。`pageTitleFor` 落到「后台管理」 | — | 难用（冻结不修，仍是坑） |
| **P-ADM-15** | 超管 | `ProductEvents.tsx:28-30,81-87` | 失败 toast+空表；`pagination={false}`；事件名要手打 `market_subscribe_succeeded`；`occurred_at` 无上海日展示 | 失败装空 | 故障 |
| **P-ADM-16** | 超管 | `NewApiOps.tsx:58-66` 渠道配置失败 `setChannelsCfg([])`；总览失败有 Alert **无重试按钮** | 71.2/71.3 文案在总览；配置表可装空 | 半套 | 故障（配置表） |
| **P-ADM-17** | 经办 | `LlmProviders.tsx:60-63` 列表 catch 空 | 「暂无 LLM 供应商，点击新建」可能是加载失败 | 失败装空 | 故障 |
| **P-ADM-18** | 任何人 | `ErrorBoundary.tsx:35-38` `subTitle={error.message}` | 渲染崩溃把异常字符串给用户；官网无边界 | 不友好 | 难用 |
| **P-ADM-19** | 租户 admin | `Members.tsx:173-174` Alert「平台级用户管理请用用户管理页」 | 去 `/users` → 404 同形（对租户正确），本页却指路 | 404 可理解，**指路错误** | 难用 |
| **P-ADM-20** | 侧栏 | `AdminLayout.tsx` Sider 无折叠/无移动端 | 七叶+五组在窄屏挤 | — | 难用（NFR-07 不要求密铺，仍不好用） |

`/llm` 经办可进、平台行写控件按 `isPlatformAdmin` —— 与 FR-06.5/73 **对齐**，不列痛点。

### 3.8 平台超管 · 运营台（登录态未活体点）

源码：租户表 `pagination={false}`（`PlatformOps.tsx:240`）；禁用无二次确认（`99-102`）；到期 DatePicker 无「清到期」说明除 label。死信 Tab 空/错相对清楚。产品事实见 P-ADM-15。待确认收款见 P-BILL-02。

---

## 4. 已不再是痛点（防把 09-08 旧诊断回流）

相对 `feat-four-pillars-v2/01-define/diagnosis/frontend.md` **已废**：

- Hero `128,000+` / Features Excel / 三档全进注册 / 注册主按钮「再注册一家」/ Login 无企业注册链
- 双广场、四 Tab「专家」、卡片不可点、catch→「暂无已发布」
- 租户 admin 进 `/newapi` 道歉 403（现 404 同形；活体匿名亦然）
- Usage 页渲染 `QUOTA_EXCEEDED` 明文
- 无「我的安装」、无七叶、无订阅弹窗宿主四态
- `is_platform_admin` 登录投影缺失（现 `ProtectedRoute` / 菜单都认）

这些 **不要** 再写进本特征的实现范围当未做。

---

## 5. stub vs 难用 vs 故障（收口）

| 档 | 条目 |
|----|------|
| **stub（合同允许没有）** | FR-51 租户自助出站拉数钥匙；在线支付/发票（Q-BILL：通道空）；Q-PRICE 未关故官网付费档保持预告；Q-AGPL 收费故事；Q-VOICE 首句不在本诊断选定 |
| **已落地但难用（018c369 + Wave 1 面）** | `/relay` 渠道组无接入说明、经办只读；用量双 CTA 且无订单列表；运营台待确认收款；租户面对七叶治理台；商店无翻页；注册邮箱≠登录名 |
| **故障（失败=空 / 说谎 / 与冻结 FR 现网相反）** | 活体 400 NFR 种子 + 夹具 404（FR-41）；Nodes/Dashboard/Data/Relay/LLM/产品事实/待确认收款 失败装空或装 0；Settings「同步官网」假成功；Members 只读写控件未藏；专家团下拉 `listAssets('expert')` |

---

## 6. 角色视角（不好用怎么被感觉到）

1. **访客**：首页精选全是 NFR 卡片「评审中」→ 市场 20/400 无翻页 → 搜 pdf / 打开 example-pdf 404 → 定价说渠道组预告。注册后可能用邮箱登录失败。
2. **负责人**：进后台要先过采集/LLM；用量满了两个下一步；下了升级单找不到；渠道组能签令牌但不知道贴到哪；官网仍说买不到渠道组。
3. **经办**：任务页工人离线仍能提交；结果「真的 0 条」；「能力市场」是治理台；渠道组菜单进得去签不了。
4. **只读**：用量能看；成员页仍显示「添加成员」。
5. **平台超管**：值班空态句大体在；运营台收款 Tab 像内部 SQL 结果；产品事实手打事件名；目录里 400 张 NFR 要人工翻。

---

## 7. 开放问题（本帽不代选）

- **Q-VOICE**：首屏那一句仍是采集 Hero，不是四柱并列（符合未关前禁令）；是否改口不在本诊断选。
- **Q-PRICE**：付费档预告 vs 后台已能提交 pro 线下单，两套事实。出口形态待选。
- **Q-MARKET-USER**：商店被性能测试种子占据，访客无法完成「搜短名订一行」。是否清种子/夹具优先，不代选。
- **Q-AGPL**：`/relay` 已发令牌，对外收费故事未写。
- **Q-OPS-COLLECT**：`contact@localhost` 使 mailto 出口空转。

---

## 8. 非结论

本文件是分诊输入，不是 PRD、不是票表。不发明「把 NFR 种子藏掉」的实现方案。塑形若重画 `/relay` 与定价闭集 B2，需先承认 Q-RELAY 已关、sitemap v1「不做 /relay」过期。
