# 本地开发环境 Web 配置（覆盖 default）
# 只覆盖 CORS ORIGINS，其他配置继承 default

CORS:
  ORIGINS:
    - "http://localhost:9112"      # 管理后台
    - "http://localhost:9113"      # 官方网站
    - "http://127.0.0.1:9112"
    - "http://127.0.0.1:9113"
