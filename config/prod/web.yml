# 生产环境 Web 配置（覆盖 default）
# 只覆盖差异部分，其他配置继承 default

CORS:
  ORIGINS:
    - "https://your-domain.com"        # 官方网站（生产环境域名）
    - "https://admin.your-domain.com"  # 管理后台（生产环境域名）
  ALLOW_HEADERS:
    - "Content-Type"
    - "Authorization"
    - "X-API-Key"
