# 生产环境配置

DEBUG: false
RELOAD: false

# === Scrapy 配置（生产环境优化）===
CONCURRENT_REQUESTS: 16
DOWNLOAD_DELAY: 0.5
LOG_LEVEL: "WARNING"

# === 性能优化 ===
CACHE_TTL: 7200  # 2小时
MAX_UPLOAD_SIZE: 52428800  # 50MB
